import { createElement as h } from 'react';
import { defineMeta } from '../../../design/meta';
import { Badge } from './Badge';
export default defineMeta({ tier: 'atom', name: 'Badge', description: 'Pill for statuses, counts and page codes. `status` keys map to lifecycle hue tokens; `violet` is the fifth model hue, `outline` the bordered tier.', props: [{ name: 'tone', type: 'BadgeTone', default: 'neutral', description: 'neutral | primary | success | warn | danger | info | accent | ink | violet | outline' }, { name: 'status', type: 'string', description: 'built | stub | live | draft | expired | backlog | doing | done | blocked | awaiting_justin' }, { name: 'size', type: "'sm'|'md'", default: 'md', description: '' }], states: ['default'],
  usages: [{ title: 'Tones', render: () => h('div', { className: 'row wrap' }, h(Badge, null, 'Neutral'), h(Badge, { tone: 'primary' }, 'HUB-01'), h(Badge, { tone: 'success' }, 'Live'), h(Badge, { tone: 'warn' }, 'Stub'), h(Badge, { tone: 'danger' }, 'Blocked'), h(Badge, { tone: 'accent' }, 'New'), h(Badge, { tone: 'ink' }, 'Fable 5.1'), h(Badge, { tone: 'violet' }, 'Fifth model'), h(Badge, { tone: 'outline' }, 'estimate')) }, { title: 'Model hues (MODEL_TONE + spare)', render: () => h('div', { className: 'row wrap' }, h(Badge, { tone: 'primary' }, 'Fable 5.1'), h(Badge, { tone: 'accent' }, 'Opus 5'), h(Badge, { tone: 'info' }, 'Sonnet 5'), h(Badge, { tone: 'warn' }, 'Justin'), h(Badge, { tone: 'violet' }, 'next model')) }, { title: 'Lifecycle statuses', render: () => h('div', { className: 'row wrap' }, ...['built', 'stub', 'live', 'draft', 'expired', 'backlog', 'doing', 'done', 'blocked', 'awaiting_justin'].map((s) => h(Badge, { key: s, status: s }, s))) }], a11y: ['Text carries the meaning; colour is never the only signal.', 'violet fg is 7.2:1 on its bg and on white (light) / 8:1 (dark).'], usedBy: ['HUB-01', 'D-01', 'D-02', 'D-07', 'K-01'] });
