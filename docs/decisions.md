# Decisions

Append-only. Statuses: `decided` / `proposed` (needs Justin) / `binding (Justin <date>)` / `superseded by D-0xx`. Reversals add a row.

| # | Date | Decision | Source | Status |
| --- | --- | --- | --- | --- |
| D-001 | 2026-09-18 | **Stack = Petrock's**: Vite 5 + React 18 + TS strict, react-router v6 HashRouter, plain CSS with generated tokens, react-markdown, @fontsource fonts, Playwright dev-only. No Tailwind / UI kit / state library. Rejected: Next.js static export (paperos) because Pages + hash routes need no server and the house pattern is Vite. | house pattern §1, prompt 0001 | decided |
| D-002 | 2026-09-18 | **GitHub Pages via Actions** (`.github/workflows/pages.yml`, checkout@v5 / setup-node@v5 / configure-pages@v5 enablement / upload-pages-artifact@v3 / deploy-pages@v4); `base: './'`. Git only: no PRs, no Slack posts, no Contents-API. | org rules | decided |
| D-003 | 2026-09-18 | **Page codes** `/^(L\|C\|B\|S\|A\|K\|W\|R\|M\|D\|HUB)-\d{2}[a-z]?$/`: L landing, C OS demo, B booking, S studio, A admin, K plan, W website, R proposal, M manual, D dev, HUB hub. Surfaces `public, demo, studio, admin, plan, dev, docs, manual`. | prompt 0001 | decided |
| D-004 | 2026-09-18 | **Roles** `super_admin, strategist, analyst, prospect, guest` with string permissions (`prospects.read/write, pages.publish, events.read, plan.edit, feedback.read/write, dev.tools, proposal.read, tables.read, docs.read, demo.open, booking.create`). Pages call `can()`, never compare roles. Dev mode super_admin only. | house pattern §2 | decided |
| D-005 | 2026-09-18 | **Module contract**: a module worker touches only `src/modules/<name>/**`, `src/data/schema|seed/<name>.ts`, `src/rules/<name>.ts`, new components, its page docs and `docs/changelog/_pending/<name>.md`; never commits, never builds, never edits shared files; built route beats stub at the same path. | CLAUDE.md | decided |
| D-006 | 2026-09-18 | **Engine is pure and shared** (`src/engine`): catalog, guessStack, savings, deriveRoleViews, pickArchetype, composePage, imagePrompts, intake (nextQuestions, applyAnswer, adaptFromEvents) with unit checks in `scripts/test-engine.mjs`. Pages render `pages.model` snapshots; they do not recompute. Rejected: computing in landing components (untestable, drifts per archetype). | prompt 0001 | decided |
| D-007 | 2026-09-18 | **MockProvider on localStorage** (`leadmagnet.db.v1`, `SEED_VERSION`) behind the `DataProvider` seam; Supabase later (T44) via the same interface; Company OS reference only. | house pattern §7 | decided |
| D-008 | 2026-09-18 | **Actions manifest = WebMCP surface and voice vocabulary**: every spec has `actions: ActionDef[]`; bus in `src/actions`; D-04 lists them; WebMCP tool generation is T45. | org rules (Control) | decided |
| D-009 | 2026-09-18 | **Tracking is one `events` table** written by `track(type, meta, ctx)` with a per-tab `sessionStorage` session id; types `view, section_view, scroll_depth, cta_click, demo_open, demo_role_switch, booking_started, booking_confirmed, form_submit, exit_intent, outreach_open, outreach_click`. | playbook principle 9 | decided |
| D-010 | 2026-09-18 | **Four archetypes** A Reveal, B Savings Audit, C Walkthrough, D Letter, composed from a shared `Section` union so sections are reusable across archetypes and A/B variants. | conversion playbook | decided |
| D-011 | 2026-09-18 | **Default archetype = A (Reveal)**; B (Audit) A/B-tested against it on cold traffic; `pickArchetype` scoring as in the playbook. | conversion playbook | proposed |
| D-012 | 2026-09-18 | **The demo is never gated** by a form; name / email asked at the moment of value ("save your workspace"). | conversion playbook 1 | decided |
| D-013 | 2026-09-18 | **Pages expire 14 days after publish** ("your workspace is live for 14 days"); expired -> L-05. | conversion playbook 8 | proposed |
| D-014 | 2026-09-18 | **Price bands** starter $249 / team $499 / multi $899 per month (starter <= 5 people & 1 location, team <= 25, multi otherwise) used in the savings math and W-03. Placeholder numbers until Justin sets pricing. | engine stack.ts | proposed |
| D-015 | 2026-09-18 | **Fonts**: Bricolage Grotesque (display) + Inter (body) via @fontsource, bundled and offline. Rejected: Google Fonts (blocked behind the proxy, external request). | house pattern §6 | proposed |
| D-016 | 2026-09-18 | **Company OS is reference only**: nothing wires into it until Justin says so (T50 blocked). Same rule as Petrock D-183. | org rules | binding (Justin, org rules) |
| D-017 | 2026-09-18 | **Image generation, LLM enrichment, booking provider, Stripe, Supabase are not wired**: interfaces + `Placeholder` (`Enricher` with `RuleEnricher` default and `LlmEnricher` unwired; `assets` rows with prompts; `bookings` mock). | prompt 0001 | decided |
| D-018 | 2026-09-18 | **Prospect theming via `--lp-*` custom properties** (`prospectStyle()`) on the landing / demo root; the Imagine tokens are never forked per prospect. | design | decided |
| D-019 | 2026-09-18 | **10-foot bands**: `--scale` 1.125 at >= 1920, 1.5 at >= 2560, 2.25 at 3840 multiplies every type and spacing token; body 16 px at >= 1920. | P-01 | decided |
| D-020 | 2026-09-18 | **Plan as data**: `tasks` table seeded from `src/data/seed/plan.ts` mirrors `docs/build-plan.md`; K- module renders Kanban / list / timeline with `depends_on` edges; model named per task. | org rules (first deliverable) | decided |
| D-021 | 2026-09-18 | **Page canvas D-07** is an in-app zoomable / pannable canvas of every route (cards grouped by surface, live iframe thumbnails on demand). Rejected: a static exported canvas HTML (Hoy) because the route manifest is live. | org rules (Hub) | decided |
| D-022 | 2026-09-18 | **Industry catalog** of 10 industries + other with real, plausible tool prices (Toast, 7shifts, Dentrix, Weave, Gingr, Mindbody, Clio, ServiceTitan, Tekmetric, Zenoti, Google Workspace, QuickBooks...). Prices are 2026 list estimates; confirmed tools override guesses. | engine catalog | decided |
| D-023 | 2026-09-18 | **esbuild (Vite's dependency) bundles the engine for the Node unit test** instead of adding a test runner; no new dependency. | scripts/test-engine.mjs | decided |
