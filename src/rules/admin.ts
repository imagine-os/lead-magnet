import type { Rule } from './index';
export const rules: Rule[] = [
  { id: 'R-A01', title: 'One funnel vocabulary, in order', description: 'The funnel is always outreach_open -> view -> demo_open -> booking_started -> booking_confirmed, counted as distinct sessions per stage so a chatty page cannot inflate a step. Every breakdown (archetype, warmth, variant) uses the same five stages in the same order; no page invents a stage.', category: 'analytics', status: 'implemented', pages: ['A-01', 'A-02'], source: 'conversion-playbook 9, prompt 0001 T15', implementedIn: 'src/modules/admin/funnel.ts FUNNEL_STAGES' },
  { id: 'R-A02', title: 'A/B needs both variants live', description: 'A variant comparison is only shown when at least two distinct pages.variant values have live pages with events; with one variant the card says so instead of implying a winner. Rates are labelled with their session counts, never shown as a winner below a usable sample.', category: 'analytics', status: 'implemented', pages: ['A-01'], source: 'P-15 (A/B on cold traffic)', implementedIn: 'src/modules/admin/FunnelPage.tsx' },
  { id: 'R-A03', title: 'Recommendations are recorded before they are applied', description: 'adaptFromEvents() output is written to `recommendations` (status proposed, with reason, score and who decided) before any page row changes; applying flips that row to applied with decided_at, and dismissing flips it to dismissed. Nothing is changed on the strength of an on-screen suggestion alone (P-08).', category: 'analytics', status: 'implemented', pages: ['A-02'], source: 'P-08, P-11', implementedIn: 'src/modules/admin/ProspectTimelinePage.tsx record()' },
];
