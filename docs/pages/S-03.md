---
title: Page composer
code: S-03
route: /studio/prospects/:id/compose
roles: super_admin, strategist
status: built
module: studio
---

# S-03 · Page composer

## Purpose

Choose the landing archetype with the scoring in front of you, see the real public route at phone and desktop width, set the A/B variant, and publish a workspace that lives 14 days. Publishing snapshots a `PageModel` into `pages.model`, so the landing page renders what was composed and never recomputes.

## Screenshots

Captured 2026-09-19 (T20) at 390 / 1280, light.

## Sections (layout order)

1. Prospect nav
2. Live page status - status, archetype, variant, days left, published date; confidence / savings / tools / slug stats
3. Archetype ranking - four cards with a score bar and the reasons from `pickArchetype()`
4. Picker - SegmentedControl, variant input, the resolved public route
5. Preview - the real route in a same-origin frame at 390 and 1280
6. Publish - the R-S01 gate when confidence is low, publish / republish, copy link, open public page, expire

## Data

| Table | Read / write | Notes |
| --- | --- | --- |
| `prospects` | read | drives the scoring and the composition |
| `stack_guesses` | read | passed into `composePage()` so the snapshot matches the confirmed stack |
| `pages` | read / write | insert on first publish, then `update(id, patch)` |

## Actions

| id | intent | permission |
| --- | --- | --- |
| `studio.pickArchetype` | "use the {archetype} archetype" | `pages.publish` |
| `studio.setVariant` | "set the variant to {variant}" | `pages.publish` |
| `studio.overrideConfidence` | "publish anyway under 30 % confidence" | `pages.publish` |
| `studio.publishPage` | "publish the page" | `pages.publish` |
| `studio.expirePage` | "expire the page" | `pages.publish` |
| `studio.copyPageLink` | "copy the public link" | any |

## Rules

- `R-S01` - publish is blocked under confidence 0.3 unless the override toggle is on
- `R-S02` - `published_at = now`, `expires_at = now + 14 days` (`PAGE_TTL_DAYS`)
- `R-C05` - the same 14 days the CTA band promises; expired pages fall to L-05
- `R-E02` - the ranking is the playbook scoring
- `R-S03` - insert then update by id; no page state lives only in memory

## Logic

- Archetype: the picked one, else the page's current one, else the top-ranked one.
- Slug: the existing page's slug wins so a published link never moves; otherwise `slugify(business-city)`.
- Publish with an existing row: one `update` with the recomposed model. First publish: `insert` (model null) then `update` with `composePage(p, archetype, { pageId: created.id, ... })`, because the snapshot carries its own page id.
- Expire sets `status: expired` and keeps the snapshot so analytics and page docs still resolve.
- Preview routes: `reveal -> /p/:slug`, `audit -> /p/:slug/audit`, `walkthrough -> /p/:slug/story`, `letter -> /p/:slug/letter` (owned by the landing module).

## Components

ViewportFrame, SegmentedControl, Card, Button, Badge, ProgressBar, Field, Input, Toggle, Stat, Chip.

## Real vs mock

Real: ranking, composition, publish, expire, the 14-day window, the public link. Mocked: nothing here - but the preview shows whatever the landing module currently renders for that route (a stub before T12 lands).

## Responsive check (P-01)

Checked at 360, 390, 768, 1280, 1920, 2560, 3840 - no horizontal overflow. The two preview frames stack under 900 px; ViewportFrame scales each frame to its container.

## Changelog

- `docs/changelog/0003-studio.md`
