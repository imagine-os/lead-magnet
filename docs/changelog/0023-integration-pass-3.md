# 0023 - Integration pass 3 (T22): v0.4.0

version: 0.4.0
date: 2026-09-19
prompt: 0004
intent: Take the six pass-3 change sets (changelogs 0017..0022: components, control, landing, admin, os-demo, catalog), resolve every cross-module request at the root instead of per page, wire the two engine follow-ups the modules could not touch (sub-industry intake, objections in `composePage()`), mount the spatial hook on every shell that had none, regenerate the generated artefacts (SQL, tokens, actions manifest, catalog doc), land every doc in the same turn and bump to v0.4.0 (no push in this task: the QA sweep and a final pass follow).
decision: (1) sorting is adopted where the requests pointed (A-03, A-05, K-02, S-01) and deliberately not on the A-01 A/B table, which stays in funnel order (R-A01); (2) the sub-industry is asked by the engine, not by the studio alone: `FIELD_WEIGHTS.sub_industry` exists, but a field that does not apply to a prospect is skipped by `nextQuestions()` and left out of the confidence denominator, so 13 industries without sub-types are not penalised (D-122); (3) the switching objections belong to the composer (D-109): four `faq` items for every prospect and a `faq` section in every archetype's order, so `Faq.tsx` renders the PageModel only; (4) 10-foot sizing is fixed in the library (`Avatar`, `Button` icon, `Chip`, `Tabs`, `Stat --stat-value-size`, chart tokens, icons) rather than by per-module hooks (D-123); (5) every shell mounts the spatial hook and the arrow-owning regions are marked, so `qa:dpad --strict` can become a gate next (D-124); (6) module decisions keep their `proposed` status with real numbers D-093..D-120; the integration's own rows are D-121..D-124.
rejected: adopting sortable headers on the A-01 comparison table (two self-normalised funnels were the misread the page was built to avoid; funnel order is the point); a `sub_industry` weight that counts against every prospect (a confidence bar that can never fill is a lie); keeping the objections in the landing strings and the engine both (two sources for one FAQ); scaling the `Button` icon through a new `iconSize` prop (CSS `width` / `height` on the SVG already win over the attributes - one rule, no API); a second ref on R-01 (the page already had a root ref for its print / scroll logic); making `qa:dpad --strict` a build gate in this pass (the report is fresh; T58 owns the gate after the sweep re-runs it on the merged tree).
files: src/modules/admin/{EventsPage.tsx, BookingsPage.tsx, admin.css}, src/modules/plan/ListPage.tsx, src/modules/studio/{ProspectsPage.tsx, fields.ts, IntakeChat.tsx}, src/engine/{intake.ts, compose.ts, index.ts}, src/modules/landing/{index.ts, ExpiredPage.tsx, landing.css, sections/Faq.tsx, sections/RoleViews.tsx}, src/modules/booking/{BookPage.tsx, ConfirmedPage.tsx}, src/modules/website/SiteChrome.tsx, src/modules/proposal/ProposalPage.tsx, src/modules/hub/{NoAccessPage.tsx, HubPage.tsx}, src/modules/dev/CanvasPage.tsx, src/modules/os-demo/DemoShell.tsx, src/components/atom/{Icon/Icon.tsx, Avatar/Avatar.css, Button/Button.css, Chip/Chip.css}, src/components/molecule/{Tabs/Tabs.css, Stat/Stat.css, PairedBarChart/PairedBarChart.css}, src/components/organism/CommandPalette/CommandPalette.tsx, src/design/tokens.ts + src/styles/tokens.css, src/data/MockProvider.ts (SEED_VERSION 4), src/data/seed/plan.ts, scripts/test-engine.mjs, supabase/schema.sql + docs/data-model.md (regenerated), docs/reference/{actions-manifest.json, voice-vocabulary.json} (regenerated), docs/reference/catalog.md (regenerated), package.json (0.4.0, `catalog:doc`), docs/* (prompt 0004, changelogs 0017..0023, decisions D-093..D-124, kanban, build-plan, surfaces, README maps, qa README, page docs) · final pass (review + sweep): scripts/{qa-a11y.mjs, qa-responsive.mjs, screenshots.mjs, frames.mjs, test-voice.mjs}, src/a11y/{voice.ts, spatial.ts, useVoice.ts}, src/actions/palette.ts, src/components/organism/{CommandPalette/CommandPalette.tsx, TopBar/TopBar.css}, src/components/atom/{Button/Button.css, IconButton/IconButton.css}, src/modules/landing/{specs.ts, hooks.ts, landing.css, sections/Faq.tsx, sections/RoleViews.tsx}, src/modules/studio/{ProspectsPage.tsx, specs.ts}, src/modules/hub/HubPage.tsx, src/modules/admin/AbReadout.tsx, src/modules/plan/plan.css, src/data/seed/{prospects.ts, plan.ts}, docs/reference/{actions-manifest.json, voice-vocabulary.json} (regenerated), docs/ops-manual/{en,es}/01..03 (re-read), docs/qa/* (regenerated), docs/screenshots/* (changed codes + routes.json), public/frames/** + public/og/* (regenerated within budget), docs/pages/L-02..L-04, docs/platform-principles.md (P-04), docs/{decisions.md D-125..D-128, kanban.md, build-plan.md, README.md, qa/README.md, reference/surfaces.md, prompts/0004}
codes: A-03 A-05 K-02 S-01 (sortable headers) · S-02 (sub-industry question) · L-01..L-04 (objections `faq` composed for every archetype) · L-05 B-01 B-02 W-01..W-03 R-01 HUB-02 (spatial hook) · D-07 L-01 (`data-spatial="skip"`) · HUB-01 C-01..C-07 L-01..L-03 (icons `mic` / `link`) · A-01 (chart tokens, seed version, SQL) · D-02 (library: `Avatar`, `Button` icon, `Chip`, `Tabs`, `Stat`, `Icon`, tokens)

## What changed

### Cross-module requests, fixed at the root
- **Sortable `DataTable` adopted** (0017 adoption notes; 0020 request 5): A-03 sorts by time / type / prospect / page / variant (`accessor`s resolve the joined rows, default newest first; the JSON action column is `srOnlyHeader`); A-05 sorts slot / prospect / duration / contact / requested / status on both tables (upcoming soonest first, past latest first); K-02 keeps its `sortCol` / `dir` state and hands it to the table as a controlled `sort` + `onSort` (the header arrows the page drew itself are gone; the th carries `aria-sort` and a 44 px button), the chip row stays as the card-mode control and `plan.sortList` still works; S-01 sorts name / business / industry / warmth / confidence and its Duplicate column is `srOnlyHeader`. The A-01 A/B comparison table stays in funnel order on purpose.
- **Spatial navigation everywhere** (0018 requests): `data-spatial="skip"` on the D-07 `.cv-view` and the landing RoleViews strip; `useSpatialNav` + `useGamepadNav` mounted on B-01, B-02, L-05 (Back = top of the page like L-01..L-04), W-01..W-03 (once, in `SiteChrome`), R-01 (on its existing root ref) and HUB-02. The first `qa:dpad` report's nine no-hook routes are now eight hooked + L-06 (a hidden static card). The hook tolerates the pages' early "no prospect" returns (the ref is simply never attached).
- **Icons**: `Icon` gains `mic` and `link` (Lucide weight, same style). `CommandPalette` renders `<Icon name="mic">` instead of its inline SVG, the hub Speak button uses `icon="mic"` (was `message`), the landing role-share button and the demo Share-this-view button use `icon="link"` (were `copy`).
- **Tokens and 10-foot sizing** (0020 request 1; 0021 requests 1, 2, 4; 0019 request 4): `--lime-700: #6B9E22` / `--lime-650: #71A825` join the ramp and `PairedBarChart.css` / `.ad-ab-dot-B` read them (the validation numbers stay in the CSS comment). `Avatar` 36 / 28 / 56 px and the `sm` initials follow `--scale`; the `Button` icon is `calc(18px * var(--scale))` (`sm`: 16) through CSS, so no prop changed; interactive `Chip` boxes are `calc(44px * var(--scale))`; `Tabs` keeps its scroller inside its host (`max-width: 100 %; min-width: 0; flex-wrap: nowrap`, tabs `flex: none`), which is what put a C-04 tab at x = 461 inside a strip that had grown past its column; `Stat` exposes `--stat-value-size` (md / lg / sm) and `landing.css` sets it on `.lp .stat-lg` in its three TV bands instead of overriding `.stat-value`.
- **Sub-industry intake** (0022 request 1; ~40 lines in `src/engine/intake.ts` + the studio select): `FIELD_WEIGHTS.sub_industry` (0.85, "What kind of {industry} is {business}?" with the industry label filled in EN / ES) sits right after the industry; `nextQuestions()` skips it when `industry(p.industry).sub` is empty; `computeConfidence(fields_known, p?)` leaves a non-applicable field out of the denominator; `applyAnswer(p, 'industry', ...)` drops a `sub_industry` the new industry does not list; `subIndustries(p)` is exported. S-02's `IntakeChat` shows the entries as a `Select` (`fieldOptions(field, t, bi, p)`, `SELECT_FIELDS` + `sub_industry`, `confidenceGain` passes the prospect). Engine check 21 covers all of it. `guessStack()` weighting `sub[].extra_tools` is T55.
- **Objections into `composePage()`** (0019 requests 2 and 3): the four switching objections are `faq` items for every prospect (EN + ES verbatim from the landing strings), and every archetype composes a `faq` section (audit and walkthrough before the CTA band, letter between the hero and the band). `Faq.tsx` renders `section.items` only; the `landing.obj_*` strings are removed (no other reader). `landing.toggleFaq` matches the composed questions as before.
- **Generated artefacts**: `SEED_VERSION` 3 -> 4 (seeded A/B splits, recomposed pages, demo / landing copy); `npm run sql` regenerated `supabase/schema.sql` + `docs/data-model.md` (`promote_variant`, and the `intake_turns` table that pass 2 had left out of the SQL); `npm run tokens` (violet pair, lime steps); `npm run actions -- export` on the merged tree: 144 tools / 227 declared rows / 51 routes (no change from the control worker's export, which already included the other workers' in-progress actions); `npm run catalog:doc` (23 industries, 24 sub-industries, 352 listings) now an npm script.

### Gates and evidence
- `npm run typecheck` clean (the `AbReadout.tsx` errors two workers saw were fixed by the admin worker before the merge; nothing was red at the start of the integration).
- `npm run test:engine` 21 / 21 (20 from the catalog pass + the sub-industry check), `npm run test:voice` 18 / 18 (144 phrases), `npm run build` green before the last commit. Final pass: `test:voice` 20 / 20 (+ `routeIdFor` by kind, apostrophe-safe `quoted`), `test:engine` 21 / 21, typecheck clean, build green before the push; the sweep numbers are under "Sweeps" below.
- Counts at v0.4.0: 51 routes (all built), 11 tables, 51 rules (+ R-L07..R-L09, R-A04), 42 components (+ CommandPalette, ConfirmDialog, DateRange, PairedBarChart), 144 unique action ids / WebMCP tools (227 declared rows at the integration commit, 230 after the review declared `landing.toggleFaq` on L-02..L-04; admin 15, booking 7, demo 15, dev 17, hub 9, landing 18, manual 6, plan 8, proposal 4, site 7, studio 38), 21 engine checks, 18 voice checks (20 after the review), 42 plan tasks (28 done, 0 doing, 6 backlog, 7 awaiting Justin, 1 blocked).
- `docs/qa/dpad-report.md` (control worker's run on the pre-merge tree, 51 routes at 1920): hook on 42 / 51, 10 primary CTAs unreached all on the nine no-hook routes, 3 dead ends all inside the D-07 canvas region; both root causes are closed in this pass and the sweep re-runs the report.

### Docs same turn
Prompt 0004 (three verbatim messages, response left for the final pass); changelogs 0017..0022 numbered from `_pending` with real decision numbers and an integration note each, `_pending/` empty but for its README; `decisions.md` D-093..D-120 (module proposals, `proposed`; D-109 decided by this pass) and D-121..D-124 (integration, `decided`); `kanban.md` (T46 T47 T52 T53 T22 done, calendar card and nine component cards closed, T55..T60 added, Awaiting Justin gains T56, D-106, D-108 and the pass-3 proposed ids); `build-plan.md` (phase 4 line, tasks table, pass 3 outcome) mirrored in `src/data/seed/plan.ts`; `surfaces.md` §1.1 / §1.2 / §1.3 / §1.4 / §1.6 / §2.2a / §3; `docs/README.md` (`reference/catalog.md`, `qa/dpad-report`), `docs/qa/README.md`, root `README.md` counts; page docs A-01 A-03 A-05 B-01 B-02 C-01 D-02 D-07 HUB-01 HUB-02 K-02 L-01..L-05 R-01 S-01 S-02 W-01..W-03 (plus the stale `_pending/{studio,manual,annotations}` links on S-0x / M-0x / D-09 repointed at 0013 / 0011 / 0012); `package.json` 0.4.0.

## Requests carried forward (kanban)
- Component cards still open after 0017: `DataTable` card-mode sort control, `Combobox`, `DeviceMockup` / `PhoneFrame` fit + caption, `Chip` ghost / dashed, `ViewportFrame` caption / error, `Placeholder` `className`, `Icon` reading `--scale` itself (then `useDemoScale()` goes), `Badge` sizes on `--scale`, `prospectById` guard, people switcher promotion, `MODEL_TONE` fifth entry, `Prose` / `Lede`, `print.css`, `recommendations.source_hash`, `plan.syncBuildPlan`, a `dev.confirm` action for voice, hold-to-talk on a remote adapter.
- T55 `guessStack()` weights `sub[].extra_tools` + studio sub-industry surfaces; T57 ES-native intents; T58 `qa:dpad --strict` in the a11y matrix; T59 admin follow-ups (`DateRange` with an `all` option on A-01 / A-03, `Stat` delta tiles, A-02 lists recorded `promote_variant` rows); T60 demo depth pass; T56 comms provider (awaiting Justin).
- QA sweep after this pass: `qa:responsive`, `qa:a11y`, `qa:dpad`, `i18n:check`, screenshots for the changed codes, `npm run frames` (the 390 phone tour shows the stacked agenda), code review on the diff.
- For Justin: D-106 (sample testimonial keep / cut), D-108 (arrow-key spatial nav on public pages at every width vs TV bands only), and the pass-3 `proposed` rows D-093..D-105, D-107, D-110..D-120.

## Kanban moves
T46, T47, T52, T53 -> done; T22 added (done); calendar overflow card closed; nine component-request cards closed; T55, T57, T58, T59, T60 -> backlog; T56 -> awaiting Justin; D-106 / D-108 -> awaiting Justin.

## Sweeps
Sonnet 5 ran the sweep on the v0.4.0 build (i18n, d-pad, frames / og, ops manual, and found the QA boot bug); Fable 5.1 reviewed the diff, fixed the twelve findings and every QA failure at the root, re-ran every matrix to zero, took the screenshots and pushed.

### Code review (Fable 5.1, medium effort, read-only on 6dc9217..8516f6d; 12 findings, all fixed at the root)
1. **high** `voice.ts` - an id-typed slot filled from the route tested the slot name against the id prefix backwards and always took the last id (`admin.openProspect { prospect: 'front-of-house' }` on `/demo/pro_maya/role/front-of-house`). `routeIdFor()` picks by kind (known prefixes, then a name that starts with the prefix, generic `id` = last, otherwise empty); `test:voice` check 19 (D-127).
2. **medium** `spatial.ts` - Up / Down were taken from `number` / `date` / `time` / `datetime-local` / `month` / `week` / `range` inputs whose native arrows work (W-03 team-size spinner). `STEPPING_INPUTS` own all four arrows like a textarea.
3. **medium** `landing/specs.ts` - `FaqSection` renders on L-02 / L-03 / L-04 but only L-01 declared `landing.toggleFaq`. Declared on all four (`faq` in their `layout` too); manifest regenerated (230 rows); page docs L-02..L-04.
4. **medium** `voice.ts` `quoted()` - an apostrophe opened a single-quoted span (`Maya's page as 'owner'` -> `s page as`). Single quotes count only at a word boundary; `test:voice` check 20 (D-127).
5. **medium** `intake.ts` callers - S-01 create and the seed computed confidence without the prospect, so `sub_industry` stayed in the denominator for 14 industries without sub-types and the first S-02 answer jumped ~8 points for free. Both write sites pass `{ industry }`; S-01 / S-02 spec text says so.
6. **medium** `RoleViews.tsx` - the share panel was keyed by role name and `owner` is both a business and a life role: the URL field opened under both cards and the life-owner link was unreachable by voice. Keyed by `${kind}-${role}`; `viewRole` / `tryAsRole` / `shareRole` accept the key as well as the name.
7. **low** `useVoice.ts` - the aborted previous recognition's `onend` nulled the new session. Both callbacks return unless `rec.current === r` (D-126).
8. **low** `CommandPalette` / `palette.ts` / `HubPage` - the never-auto-listen guard relied on `navigator.userActivation` (absent in older Safari / Firefox, transient for ~5 s). `openPalette({ voice, fromGesture })`: only the Speak button and the trusted Ctrl/Cmd+K set it; an agent / CLI `hub.voiceListen` opens the palette with the Speak button focused (D-126).
9. **low** `AbReadout.tsx` - two activations before the `recommendations` row came back inserted two `promote_variant` rows. An in-flight promise ref dedupes.
10. **low** `CommandPalette` - `matchPhrase(..., { limit: 12 })` ran before `can()`, so a guest's 12 slots were spent on staff actions. Matches 64, filters, then shows 12; "hidden for your role" counts the filtered ones.
11. **low** `Faq.tsx` - `landing.toggleFaq` matched only the UI language; it matches EN and ES again.
12. **low** `hooks.ts` `useExitIntent` - the coarse-pointer history entry was never consumed when the intent fired by idle / flick, so Back needed two presses. Consumed on fire and on unmount when it is still ours.

### Accessibility and responsive (Sonnet 5 found the boot bug, Fable 5.1 fixed the findings)
- The sweep's matrices never finished (killed at 9 / 51 and 20 / 51; the on-disk reports were the pass-2 baseline). First complete run on the reviewed build, 51 routes x 390 / 1280: **10 axe violation nodes (color-contrast x 4 routes), 0 focus failures, 110 hit-target failures**; `qa:responsive` **714 cells / 0 failing**.
- Hit targets (110 -> 0): every one was the `TopBar` at 390 on the 18 `DesktopShell` routes - the pass-3 Commands button made the row overflow and flex shrank the three 48 px icon buttons to 41-43 px. `IconButton` is `flex: none`; the title truncates; the bar tightens to 4 px gaps under 600 px (D-128).
- Contrast (10 -> 0): the role card's "Business" badge (`--lp-primary-text` on `--lp-tint-2`) was 4.17:1 on Maya's palette -> `--lp-primary-on-tint` at 60 % (4.95:1), seen on S-03's 1280 preview and W-01's phone preview; the D-02 `tone="prospect"` link button used the raw brand primary (4.16:1) -> `--lp-primary-text` fallback chain (6.02:1); K-01's empty lane faded its heading to 60 % opacity -> quiet border and icon instead (D-128).
- Checker (D-125): the second run, three Chromiums in parallel, reported 157 contrast nodes (whole landing sections inside S-03 / W-01 iframes measured mid-fade) and four `btn-sm` "x44" targets that moved between buttons and never reproduced alone (43.99999 under a fractional `translateY` mid-transition). `qa:a11y` now settles the page (iframes booted, reveal-pending sections revealed, finite animations finished, 4 s cap) before axe and the walk and reads targets to 0.1 px; that removed every host-page false positive and the moving `btn-sm` targets. What the cap cannot remove: the S-03 / W-01 preview frames reveal their sections on the frame's own throttled timers, so the fade can still be under way when axe reads the frame; recorded as the residue rule in the report header (pass 2 did the same for inline text and iframes, D-091). The one real defect it uncovered was fixed: the CTA band's primary button read the raw brand primary on the prospect's background (3.08:1 on Maya's palette) and now reads `--lp-primary-text` (D-128). Final run, alone on the final build: **0 focus failures, 0 hit-target failures, 521 axe colour-contrast nodes - every one inside the landing preview iframes on S-03 and W-01 (both widths), judged checker timing under the residue rule printed in the report header (the same nodes pass on L-01..L-04 and in a standalone probe); 0 findings on the other 49 routes**.
- Ops-manual live data: `/#/manual` renders 7 live blocks, `/manual/intake` 1, `/manual/compose` 1, 0 unknown directives, 0 console errors (preview, after the chapter edits).

### D-pad rehearsal (`npm run qa:dpad`, 51 routes at 1920, final build)
- Hook on **49 / 51** (42 before the integration): no hook on L-06 (hidden OG card, by design, D-124) and B-02 - a fixture false negative: the default `pro_maya` has no `bookings` row, so `ConfirmedPage` returns its `EmptyState` before the spatial root mounts (kanban card).
- Dead ends **0** (3 before, all inside the D-07 canvas). Primary CTAs never reached **3** (10 before): L-05 "Request a fresh workspace" behind the empty `Textarea` arrow-owner (structural, kanban card), B-02 "Pick a time" (the fixture above), L-06 "Open the page" (no hook by design). Average coverage **67 %** (55 % before); 106 arrow-owning controls met; seven routes hit the 400-step budget (A-04, A-05, K-01, K-02, M-01, D-04, D-05 - long tables and boards, not traps).

### Spanish audit (`npm run i18n:check`, Sonnet 5)
35 `en === es` flags across 13 tables before and after the pass, every one in the D-081 set: domain loanwords (Outreach, Intake, Demo, Kanban, Prompt(s), Assets, Roles, Slug, Hero, Display / Serif / Mono, Starter / Team / Multi), true cognates (comparable, idea, Plan), proper nouns (Imagine, LinkedIn), notation (B − A, Ctrl / Cmd + K, Id, the manual's front-matter keys) and in-app loanwords already used identically elsewhere (Tokens:, Chat, widgets). No real gap; no new loanword introduced by the final pass (the new strings are code comments and spec text).

### Frames / og budget
`npm run og` (3 slugs) and `npm run frames` (3 prospects, 24 phone + 12 desktop each, the new stacked-calendar 390 tour) ran clean after the boot fix. At the sweep's default quality the two folders were **5.30 MB** (5,002,439 + 294,566 bytes), over the 5 MB line; re-run with `--quality=70 --desk-quality=47`: **4.79 MB** (4,496,250 + 294,566 bytes, 114 frame files), og regenerated on the final build. `docs/qa/README.md` and the kanban card carry the budget.

### Ops manual re-read (Sonnet 5, all five chapters EN + ES against pass 3)
- **01 How Lead Magnet works**: the "Real today" list lacked the actions bus, the command palette, voice, the d-pad and the A-01 A/B readout; "and, soon, a voice" was false once T46 shipped. Both fixed; the input list names mouse / keyboard / touch or pen / remote d-pad / voice through one actions bus. Version 0.3.0 -> 0.4.0.
- **02 Intake and research**: the field-weight list omitted `sub_industry` (0.85, between first name and team size) and the rule that it is skipped, and left out of the denominator, for industries without sub-types. Added. Version bumped.
- **03 Compose and publish**: the Letter archetype was described as going straight from the note to the CTA; since D-109 every archetype, the letter included, composes the four switching objections between hero and CTA band. Fixed. Version bumped.
- **04, 05**: read in full, nothing false (neither mentions sortable tables, the readout, sub-industries, share links or the d-pad); front matter stays 0.3.0 on purpose - a chapter's version is the last release that changed its content.
- Out of the manual's scope, fixed in the final pass: `docs/platform-principles.md` P-04 still listed `useSpatialNav` and the voice controller as queued.

### QA script fix (Sonnet 5, D-125)
`qa-responsive.mjs`, `qa-a11y.mjs`, `screenshots.mjs` and `frames.mjs` all waited on `#root > *`. Since changelog 0018 `ControlBridge` mounts `CommandPalette` -> `Modal` -> a closed native `<dialog>` as the first child of `#root` on every route, and Playwright's non-strict `waitForSelector` locks onto the first match, so every page load ran into the 10 s timeout (a 51 x 7 x 2 matrix could not finish). The four selectors are `#root > *:not(dialog)`: ~0.4 s per load (367 ms in the probe). `qa-dpad.mjs` and `og.mjs` were unaffected (they wait on `window.__leadmagnet.routes` / `.og-card`). The final pass added the settle wait and the 0.1 px target rule to `qa-a11y.mjs` (above).

### Screenshots (final build)
`npm run screenshots` for the changed codes (L-01..L-05 and W-01 retaken after the CTA-band fix) A-01 A-03 A-05 B-01 B-02 C-01..C-07 D-02 D-04 D-07 HUB-01 HUB-02 K-02 L-01..L-05 R-01 S-01 S-02 W-01..W-03 at 390 / 1280, dark for HUB-01 L-01 C-01 A-01, the 7-width set for HUB-01 L-01 C-01 A-01: **144 files**, `docs/screenshots/routes.json` regenerated. The palette-open capture on HUB-01 is skipped: the script has no interaction hook (`--label=` only names the file).

### Gates at the push
`npm run build` green (tokens -> tsc -> vite); `test:engine` 21 / 21; `test:voice` 20 / 20 (144 phrases); `i18n:check` 35 flags, all documented; `qa:responsive` 714 / 0; `qa:a11y` 0 focus failures, 0 hit-target failures, 521 axe colour-contrast nodes - every one inside the landing preview iframes on S-03 and W-01 (both widths), judged checker timing under the residue rule printed in the report header (the same nodes pass on L-01..L-04 and in a standalone probe); 0 findings on the other 49 routes; `qa:dpad` 49 / 51 hooked, 0 dead ends, 3 unreached primaries (all named above), 67 % coverage; frames + og 4,790,816 bytes; code review 12 / 12 fixed. Code commit 1bc1699; the docs commit follows it.
