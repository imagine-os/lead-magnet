---
title: Funnel overview
code: A-01
route: /admin
roles: super_admin, strategist, analyst
status: built
module: admin
---

# A-01 · Funnel overview

## Purpose

The staff home for analytics: whether outreach is opened, whether the page converts to the demo, and whether the demo converts to a booked call - all in one vocabulary, with breakdowns that cannot disagree with each other because they share the counting code.

## Screenshots

| 390 | 1280 |
| --- | --- |
| ![390](../screenshots/A-01/390.jpg) | ![1280](../screenshots/A-01/1280.jpg) |

Captured 2026-09-19 (T20) at 390 / 1280.

## Sections (layout order)

1. Filters - archetype chips, warmth chips
2. KPI row - prospects, live pages, page views, demo opens, bookings, view→demo, demo→booking
3. Funnel chart - the five stages as an ordinal bar chart with a table view in a disclosure
4. By archetype / by warmth - the same five stages as `DataTable`s
5. A/B by variant - shown only when two variants are live (R-A02)
6. Recent activity - last 12 events with icon, time, summary and prospect link

## Data

| Table | Read / write | Notes |
| --- | --- | --- |
| `events` | read | the funnel itself |
| `pages` | read | join for archetype and variant |
| `prospects` | read | join for warmth; KPI scope |
| `bookings` | read | the bookings KPI (rows, not events) |

## Actions

| id | intent | permission |
| --- | --- | --- |
| `admin.filterFunnel` | "filter the funnel by {archetype} and {warmth}" | `events.read` |
| `admin.openProspect` | "open the timeline for {prospect}" | `prospects.read` |
| `admin.openStudio` | "open the studio" | `pages.publish` |

## Rules

- `R-A01` - `FUNNEL_STAGES` is the only stage vocabulary and every breakdown uses it in order
- `R-A02` - the variant table is replaced by an `EmptyState` naming the live variants until two exist
- `R-C06` - the page only reads what `track()` wrote

## Logic

- A stage counts **distinct `session_id`**, so a page firing six `section_view`s is still one viewer. `src/modules/admin/funnel.ts` is pure and shared with A-02.
- Archetype and variant join through `events.page_id`; warmth joins through `events.prospect_id`.
- Conversion labels return an em dash rather than dividing by zero.
- Chart: ordinal single-hue ramp (`FunnelChart`), light mode `electric-400 → ink-700`, dark mode re-stepped `electric-700 → electric-200`; both validated for monotone lightness, step gaps and light-end contrast against their own surface. Labels, values and step percentages are text in text tokens, so nothing is colour-only.

## Components

`Stat`, `FunnelChart` (new, molecule), `DataTable`, `Chip`, `Card`, `Badge`, `EmptyState`, `Button`, `Icon`.

## Real vs mock

Everything on this page is real: it reads the rows that landing pages, the demo and B-01 actually wrote. Seeded traffic comes from `src/data/seed/prospects.ts`. Nothing is estimated or extrapolated.

## Responsive check (P-01)

Declared at 360, 390, 768, 1280, 1920, 2560, 3840. Under 720 px the funnel rows stack label / value / bar / percentage; under 900 px the recent list collapses to three rows per item. `DataTable` carries its own card mode. 10-foot: KPI values use the display face at `--fs-2xl` and scale with `--scale` bands.

## Changelog

- `docs/changelog/0007-admin.md`
