# Decisions

Append-only. Statuses: `decided` / `proposed` (needs Justin) / `binding (Justin <date>)` / `superseded by D-0xx`. Reversals add a row.

| # | Date | Decision | Source | Status |
| --- | --- | --- | --- | --- |
| D-001 | 2026-09-18 | **Stack = Petrock's**: Vite 5 + React 18 + TS strict, react-router v6 HashRouter, plain CSS with generated tokens, react-markdown, @fontsource fonts, Playwright dev-only. No Tailwind / UI kit / state library. Rejected: Next.js static export (paperos) because Pages + hash routes need no server and the house pattern is Vite. | house pattern §1, prompt 0001 | decided |
| D-002 | 2026-09-18 | **GitHub Pages via Actions** (`.github/workflows/pages.yml`, checkout@v5 / setup-node@v5 / configure-pages@v5 enablement / upload-pages-artifact@v3 / deploy-pages@v4); `base: './'`. Git only: no PRs, no Slack posts, no Contents-API. | org rules | decided |
| D-003 | 2026-09-18 | **Page codes** `/^(L\|C\|B\|S\|A\|K\|W\|R\|M\|D\|HUB)-\d{2}[a-z]?$/`: L landing, C OS demo, B booking, S studio, A admin, K plan, W website, R proposal, M manual, D dev, HUB hub. Surfaces `public, demo, studio, admin, plan, dev, docs, manual`. | prompt 0001 | decided |
| D-004 | 2026-09-18 | **Roles** `super_admin, strategist, analyst, prospect, guest` with string permissions (`prospects.read/write, pages.publish, events.read, plan.edit, feedback.read/write, dev.tools, proposal.read, tables.read, docs.read, demo.open, booking.create`). Pages call `can()`, never compare roles. Dev mode super_admin only. | house pattern §2 | decided |
| D-005 | 2026-09-18 | **Module contract**: a module worker touches only `src/modules/<name>/**`, `src/data/schema|seed/<name>.ts`, `src/rules/<name>.ts`, new components, its page docs and `docs/changelog/_pending/<name>.md`; never commits, never builds, never edits shared files; built route beats stub at the same path. | CLAUDE.md | decided |
| D-006 | 2026-09-18 | **Engine is pure and shared** (`src/engine`): catalog, guessStack, savings, deriveRoleViews, pickArchetype, composePage, imagePrompts, intake (nextQuestions, applyAnswer, adaptFromEvents) with unit checks in `scripts/test-engine.mjs`. Pages render `pages.model` snapshots; they do not recompute. Rejected: computing in landing components (untestable, drifts per archetype). | prompt 0001 | decided |
| D-007 | 2026-09-18 | **MockProvider on localStorage** (`leadmagnet.db.v1`, `SEED_VERSION`) behind the `DataProvider` seam; Supabase later (T44) via the same interface; Company OS reference only. | house pattern §7 | decided |
| D-008 | 2026-09-18 | **Actions manifest = WebMCP surface and voice vocabulary**: every spec has `actions: ActionDef[]`; bus in `src/actions`; D-04 lists them; WebMCP tool generation is T45. | org rules (Control) | decided |
| D-009 | 2026-09-18 | **Tracking is one `events` table** written by `track(type, meta, ctx)` with a per-tab `sessionStorage` session id; types `view, section_view, scroll_depth, cta_click, demo_open, demo_role_switch, booking_started, booking_confirmed, form_submit, exit_intent, outreach_open, outreach_click`. | playbook principle 9 | decided |
| D-010 | 2026-09-18 | **Four archetypes** A Reveal, B Savings Audit, C Walkthrough, D Letter, composed from a shared `Section` union so sections are reusable across archetypes and A/B variants. | conversion playbook | decided |
| D-011 | 2026-09-18 | **Default archetype = A (Reveal)**; B (Audit) A/B-tested against it on cold traffic; `pickArchetype` scoring as in the playbook. | conversion playbook | proposed |
| D-012 | 2026-09-18 | **The demo is never gated** by a form; name / email asked at the moment of value ("save your workspace"). | conversion playbook 1 | decided |
| D-013 | 2026-09-18 | **Pages expire 14 days after publish** ("your workspace is live for 14 days"); expired -> L-05. | conversion playbook 8 | proposed |
| D-014 | 2026-09-18 | **Price bands** starter $249 / team $499 / multi $899 per month (starter <= 5 people & 1 location, team <= 25, multi otherwise) used in the savings math and W-03. Placeholder numbers until Justin sets pricing. | engine stack.ts | proposed |
| D-015 | 2026-09-18 | **Fonts**: Bricolage Grotesque (display) + Inter (body) via @fontsource, bundled and offline. Rejected: Google Fonts (blocked behind the proxy, external request). | house pattern §6 | proposed |
| D-016 | 2026-09-18 | **Company OS is reference only**: nothing wires into it until Justin says so (T50 blocked). Same rule as Petrock D-183. | org rules | binding (Justin, org rules) |
| D-017 | 2026-09-18 | **Image generation, LLM enrichment, booking provider, Stripe, Supabase are not wired**: interfaces + `Placeholder` (`Enricher` with `RuleEnricher` default and `LlmEnricher` unwired; `assets` rows with prompts; `bookings` mock). | prompt 0001 | decided |
| D-018 | 2026-09-18 | **Prospect theming via `--lp-*` custom properties** (`prospectStyle()`) on the landing / demo root; the Imagine tokens are never forked per prospect. | design | decided |
| D-019 | 2026-09-18 | **10-foot bands**: `--scale` 1.125 at >= 1920, 1.5 at >= 2560, 2.25 at 3840 multiplies every type and spacing token; body 16 px at >= 1920. | P-01 | decided |
| D-020 | 2026-09-18 | **Plan as data**: `tasks` table seeded from `src/data/seed/plan.ts` mirrors `docs/build-plan.md`; K- module renders Kanban / list / timeline with `depends_on` edges; model named per task. | org rules (first deliverable) | decided |
| D-021 | 2026-09-18 | **Page canvas D-07** is an in-app zoomable / pannable canvas of every route (cards grouped by surface, live iframe thumbnails on demand). Rejected: a static exported canvas HTML (Hoy) because the route manifest is live. | org rules (Hub) | decided |
| D-022 | 2026-09-18 | **Industry catalog** of 10 industries + other with real, plausible tool prices (Toast, 7shifts, Dentrix, Weave, Gingr, Mindbody, Clio, ServiceTitan, Tekmetric, Zenoti, Google Workspace, QuickBooks...). Prices are 2026 list estimates; confirmed tools override guesses. | engine catalog | decided |
| D-023 | 2026-09-18 | **esbuild (Vite's dependency) bundles the engine for the Node unit test** instead of adding a test runner; no new dependency. | scripts/test-engine.mjs | decided |
| D-024 | 2026-09-19 | **The plan timeline is a dependency graph, not a Gantt**: phases are columns, `depends_on` are edges, no date axis; the only length metric is the critical path (longest dependency chain, in tasks). Rejected: a calendar / Gantt with estimated durations; a projected finish date from task counts. | T10 plan (changelog 0002), R-K01..R-K04 | proposed |
| D-025 | 2026-09-19 | **A task cannot be set to `done` while any dependency is not `done`** (R-K01), enforced on the write in K-01 and K-04; `blocked` and `awaiting_justin` are always allowed so a human blocker is recorded honestly. Rejected: warn only; blocking those two too. | T10 plan, R-K01 | proposed |
| D-026 | 2026-09-19 | **Kanban moves are a status `Select` plus prev / next lane buttons**; drag-and-drop may be added later but never as the only affordance (P-03, remote / gamepad / voice in T46 / T47). Rejected: drag-only. | T10 plan, P-03 | proposed |
| D-027 | 2026-09-19 | **Only `status` and `done_at` are editable from the product**; the plan's shape (titles, codes, models, phases, dependencies) stays authored in `docs/build-plan.md` + `src/data/seed/plan.ts` until the docs are generated from the table. Rejected: full CRUD on tasks in the UI (two sources of truth). | T10 plan | proposed |
| D-028 | 2026-09-19 | **Publishing needs 30 % confidence or an explicit, recorded override** (R-S01, `MIN_PUBLISH_CONFIDENCE`). A page composed from a profile we barely know reads as spam; the override is deliberate and surfaced in the toast. | T11 studio (changelog 0003) | proposed |
| D-029 | 2026-09-19 | **The studio never renders landing sections itself**: S-03 previews the real public route in a same-origin `ViewportFrame`, so there is exactly one renderer per archetype. | T11 studio | decided |
| D-030 | 2026-09-19 | **A published slug is frozen**: republishing reuses `pages.slug`; only the first publish derives it from `slugify(business-city)`, so a sent link never moves. | T11 studio | decided |
| D-031 | 2026-09-19 | **Outreach sending stays unwired; "mark as sent" is an explicit demo action** that says so in its toast, so funnel numbers are never silently fake. | T11 studio, D-017 | decided |
| D-032 | 2026-09-19 | **The archetype in the URL wins over the snapshot**: `/p/:slug/audit` renders the audit archetype even when the published page is a reveal, recomposing from the same inputs (R-L01). Keeps all four variants demoable per prospect and makes A/B links trivial; the cost is that a recomposed variant is not the exact bytes a strategist published. Rejected: only the published archetype reachable, others redirect. | T12 landing (changelog 0004) | proposed |
| D-033 | 2026-09-19 | **`R-C05` (14-day workspace) is `implemented`**: expired and unknown slugs render L-05 in place (no redirect), with the real `expires_at` in days and as a date; `PAGE_TTL_DAYS` in `src/rules/studio.ts` is the one source of the number (D-013 still needs Justin for the value). | T12 landing, T11 studio | decided |
| D-034 | 2026-09-19 | **A prospect page opens in the prospect's language**: with no stored EN / ES choice, `/p/:slug`, `/book/:id` and `/demo/:id` default to `prospects.lang` (`useDefaultLang`); the toggle wins forever after and only explicit toggles persist. | T12 landing, T20 (`I18nProvider`) | proposed |
| D-035 | 2026-09-19 | **All landing motion lives under `prefers-reduced-motion: no-preference` and every scroll-driven interaction has a button equivalent** (R-L02, R-L03); the story is sticky, never scroll-jacked; `useScrollFrames(count)` is the seam T41 fills with generated frame sequences. | T12 landing, P-03 | decided |
| D-036 | 2026-09-19 | **The OS demo owns its chrome and maps `--lp-*` onto the semantic tokens on its root** (R-D01), so `Card` / `Button` / `DataTable` become the prospect's without forking the design system (P-02). Rejected: forking components per prospect; a second token file. | T13 os-demo (changelog 0005) | decided |
| D-037 | 2026-09-19 | **The demo's active role is URL state** (`/demo/:id/role/:slug`), not component state: shareable, bookmarkable, back-button-able, and `demo_role_switch` becomes a real funnel signal. Slugs are unique per view: business roles keep the bare slug, a life role that collides gets `life-<slug>` (`viewSlug`). | T13 os-demo, T20 | decided |
| D-038 | 2026-09-19 | **Demo sample content is deterministic, seeded from the prospect id**: stable screenshots and QA diffs, and two prospects in one industry still differ. Rejected: random generation at render. | T13 os-demo | decided |
| D-039 | 2026-09-19 | **Booking is 15 minutes, slot before details, mock availability until T42**: `bookings` rows are real from day one (status `requested`), availability is a deterministic FNV-1a hash of prospect id + day + time so QA and screenshots are reproducible, and the provider (Cal.com / Calendly) is swapped in behind `src/engine/slots.ts` without touching the pages. | T14 booking (changelog 0006), R-B01..R-B03 | proposed |
| D-040 | 2026-09-19 | **Prospect timezone is guessed from `prospects.city`** (city -> IANA map, country fallback, then `America/New_York`), resolved DST-correct through `Intl` with no date dependency; the abbreviation is printed next to every time so a wrong guess is visible and fixed by fixing the city. | T14 booking | decided |
| D-041 | 2026-09-19 | **Walkthrough hours are 09:00-17:00 in the prospect's local time, Monday to Friday, lunch hour blocked.** This is our availability, not the prospect's: Justin to confirm. | T14 booking | proposed |
| D-042 | 2026-09-19 | **A funnel stage counts distinct sessions, not raw events**, and `FUNNEL_STAGES` (`outreach_open -> view -> demo_open -> booking_started -> booking_confirmed`) is the only stage vocabulary; every breakdown (archetype, warmth, variant) shares the same counting code (R-A01). | T15 admin (changelog 0007) | proposed |
| D-043 | 2026-09-19 | **A/B is not shown until two variants are live**; A-01 names the live variants instead of implying a winner, and rates are always shown with their session counts (R-A02, P-15). | T15 admin | proposed |
| D-044 | 2026-09-19 | **Engine recommendations are rows before they are actions**: `recommendations` table, written as `proposed` with reason, score and decider before any page changes, then `applied` / `dismissed` with `decided_at` (R-A03, P-08). Nothing changes a live page on the strength of an on-screen suggestion alone. | T15 admin | decided |
| D-045 | 2026-09-19 | **Charts are hand-built inline SVG against the token ramps, no charting dependency**: ordinal data gets one hue with monotone lightness steps validated per theme; categorical data the fixed slot order; never a dual axis; every value is also text and every chart has a table view. Rejected: Recharts / d3. | T15 admin, dataviz method, P-02 | decided |
| D-046 | 2026-09-19 | **Board columns are moved with a `Select`, never a drag** (draft / sent / opened / clicked / replied, with `scheduled` and `bounced` collected in "other" so no row disappears). | T15 admin, P-03 | decided |
| D-047 | 2026-09-19 | **The client proposal is a printable document**: R-01 prints from the browser (no PDF dependency), chrome hidden, one section per page, black text on white; future document surfaces follow the same pattern (R-R02). | T16 proposal (changelog 0008) | decided |
| D-048 | 2026-09-19 | **Proposal and website numbers are engine-only** (R-R01, R-W01): no price, cost or saving is typed into R-01 or W-0x; changing D-014 changes every proposal and the pricing page with no page edit. Dev mode labels the bands as proposed while D-014 is unconfirmed. | T16 proposal + website | decided |
| D-049 | 2026-09-19 | **The public website is a module like any other** (`src/modules/website`, public surface, own chrome, library components only) rather than a separate static site. | T16 website | proposed |
| D-050 | 2026-09-19 | **Purchase intent is recorded before payment exists**: Choose plan writes a `form_submit` event with the band and calculator inputs; the checkout button stays a marked `Placeholder` until Stripe (T51). | T16 website | proposed |
| D-051 | 2026-09-19 | **One slot generator for the whole funnel**: `src/engine/slots.ts` (pure, unit-checked) is imported by the landing `booking_inline` section, B-01 / B-02 and A-05. The landing preview is `previewSlots()` of the same grid (three free slots per open day, prospect timezone), so every slot a landing page offers exists on B-01; `?slot=` accepts any valid instant and marks one outside the grid as `requested` instead of swapping it. Rejected: two generators kept in sync by convention (they had already diverged: 9:30 / 13:00 / 16:30 local vs the hashed 15-minute grid). | T20 integration (changelog 0009) | decided |
| D-052 | 2026-09-19 | **`bookings` carries real contact columns** (`contact_name`, `contact_email`, `contact_phone` null) instead of concatenating them into `notes`; `SEED_VERSION` 2 reseeds stale browsers (rows a person created are carried over). | T20, booking + admin requests | decided |
| D-053 | 2026-09-19 | **`useActions` handlers always see the latest render**: registration is once per page code, but each handler is a stable wrapper over a `useLatest` ref (exported from `src/actions`). D-008 makes the manifest the WebMCP surface, so a stale closure is a correctness bug. Per-module workarounds may be deleted as they are touched. | T20, plan + landing requests | decided |
| D-054 | 2026-09-19 | **Justin's vision images are references, not instructions** (`docs/reference/vision/`, prompt 0002): they illustrate the target experience and Claude-reported statuses; tool-stack guesses, savings and archetype tests in them still need prospect data and measurement. | prompt 0002 | decided |
| D-055 | 2026-09-19 | **Version 0.2.0 = the first full pass**: 50 routes built (0 stubs), 10 tables, 38 rules, 37 components, 175 declared action rows (117 unique ids); polish passes T30..T33 and providers T40+ follow. | T20 (changelog 0009) | decided |
| D-056 | 2026-09-19 | **Stack guesses carry a `likely` / `possible` tier derived from confidence** (`>= 0.5` is likely; a confirmed tool is always likely). The tier is computed, never stored, so it can be retuned without a migration. | studio 0.3.0, engine | proposed |
| D-057 | 2026-09-19 | **One guessed line per replacement module.** If two catalog products would be replaced by the same module, only the most likely one is billed - the savings number must never double-count a replacement. | studio 0.3.0, engine | proposed |
| D-058 | 2026-09-19 | **Rare products (prevalence < 0.3) are not guessed for teams under 15.** Below that we ask in the intake instead of inventing a line item. | studio 0.3.0, engine | proposed |
| D-059 | 2026-09-19 | **Pasted facts are proposals with evidence, never writes** (R-S05). The extractor is pure rules; the LLM path stays a Placeholder until T43. | studio 0.3.0, P-09 | proposed |
| D-060 | 2026-09-19 | **Every intake answer is recorded as an `intake_turns` row with its source** (R-S06), so a rule-filled profile can always be told apart from a confirmed one. | studio 0.3.0, P-09 | proposed |
| D-061 | 2026-09-19 | **An A/B test is exactly two live `pages` rows on one slug (A and B)**, each with its own archetype and snapshot; `?variant=` forces a side and variant A is "the" page for everything else (R-S04). | studio 0.3.0, playbook 10 | proposed |
| D-062 | 2026-09-19 | **Duplicating a prospect copies the profile and never the page, events or bookings** (R-S07), so a demo copy can never show someone else's traffic as its own. | studio 0.3.0 | proposed |
| D-063 | 2026-09-19 | **The industry catalog is a product asset, not sample data**: 22 industries with real product names and list prices, EN + ES, checked by `npm run test:engine`. Prices are reviewed every pass and dated in this changelog. | studio 0.3.0 | proposed |
| D-064 | 2026-09-19 | **WebMCP registration = `navigator.modelContext.registerTool` only, feature-detected, once per tool name; the window surface (`window.__leadmagnet.tools / runAction / vocabulary / webmcp`) is always published and is the entry point Playwright, the CLI and the voice controller use.** Rejected: `provideContext` (clears other scripts' tools, dropped from the 2026 proposal revision); depending on the browser API for tests. | T45, control.md §2 | proposed |
| D-065 | 2026-09-19 | **Idempotency guard at the surface**: identical `(action id, stableHash(params))` within 1.5 s returns the first call's result with `deduped: true`; handlers are still written to set, not toggle, where the data allows. Rejected: per-handler guards (117+ places to forget). | T45 | proposed |
| D-066 | 2026-09-19 | **A tool call may open its host page**: when no handler is live, `execute` navigates to a host the role can open (route params from the call, else the first seeded row), waits up to 4 s for registration, then runs. Every declared action is therefore callable from anywhere; the UI moving is the honest side effect. Rejected: `ok: false, open the page first` (was the 0.2.0 behaviour; useless to an agent). | T45 | proposed |
| D-067 | 2026-09-19 | **Spatial navigation scope**: geometric nearest-in-direction, no wrap, container-scoped hook (`useSpatialNav`) mounted on HUB-01 and the demo shell first; inert inside arrow-owning controls, `[data-spatial="skip"]` regions and open dialogs; gamepad maps onto the same handlers. Other shells follow after K-03 marks its graph. Rejected: a global listener on `document.body` (would fight DataTable, Tabs, the canvas and the graph). | T47 groundwork, P-04 | proposed |
| D-068 | 2026-09-19 | **Ops manual chapters live as markdown in `docs/ops-manual/<lang>/NN-slug.md`, not as strings in TSX.** The app is the reader; the repo is the source. Front matter carries the mandatory six keys (`title, role, part, version, updated, summary`). Rejected: prose in `useT()` keys (unreviewable), MDX (a dependency, and unreadable in the D-06 viewer). | T33, house pattern (Hoy chapter front matter) | proposed |
| D-069 | 2026-09-19 | **R-M01: a number the system owns is never typed into a chapter.** Counts, routes, roles, prices, archetype scores and KPIs appear only as live-data directives rendered from the running app. Prose may explain a number, never state it. Rejected: hand-written numbers with a "last checked" date. | T33, P-11 | proposed |
| D-070 | 2026-09-19 | **R-M02: every chapter is an `en` / `es` filename pair, and Spanish is a faithful translation, not a stub.** A missing mirror falls back to English, shows an "English fallback" badge and is listed on M-01. Rejected: shipping English-only chapters and treating Spanish as a later pass (P-13 says from the start). | T33, P-13 | proposed |
| D-071 | 2026-09-19 | **`> DECISION NEEDED:` in a chapter is the manual's half of "Awaiting Justin".** Blocks render inline and are collected on M-01; five are open today (calendar provider, automatic vs on-demand AI enrichment, A/B on one slug vs two links, which sending provider first, paid single-role pilot as a standard next step). | T33, house pattern | proposed |
| D-072 | 2026-09-19 | **Annotations anchor to an element, never to a coordinate.** `element_path` is a short selector that stops at the nearest `id` / `[data-component]`; a pin whose element is gone is listed in the drawer, never drawn at a stale position (R-F03). Rejected: canvas overlays and absolute coordinates (break on every reflow and every viewport). | T49, P-08 | proposed |
| D-073 | 2026-09-19 | **R-F01 is enforced in code, not by habit**: D-09 refuses `status = done` while `triage` or `decision_ref` is empty. An agent records why before it changes anything. | T49, P-08 house-pattern workflow | proposed |
| D-074 | 2026-09-19 | **R-F02 author weight**: Justin / super admin binding, staff tester request, prospect / guest signal - with kind cutting across it (a reproducible bug is fixed whoever files it). Encoded once in `src/rules/annotations.ts` and shown per row; a suggestion, never an autopilot. | T49, house pattern | proposed |
| D-075 | 2026-09-19 | **No `screenshot_url` on `feedback`** (the P-08 sketch had one): browser screenshot capture is not wired (T40), and `element_path` + `viewport` + `theme` has been enough to reproduce. Revisit when the image provider lands. | T49 | proposed |
| D-076 | 2026-09-19 | **`data-component` on component roots is foundation work**, not a module worker's: the AnnotationLayer prefers the attribute and falls back to a class-hint table until the sweep happens. | T49, module contract | decided (T20 pass 2: on every library root, changelog 0016) |
| D-077 | 2026-09-19 | **Video-on-scroll is screenshots of the running product, committed as static files.** `npm run frames` walks the live OS demo and writes the JPEG sequences to `public/frames/<prospectId>/`; landing pages scrub them and fall back to the live `MiniOs` composition when a sequence is missing. Rejected: generated / AI video (T40 not wired; a rendered fake of a product that exists), a `<video>` element (a JPEG sequence scrubs, seeks and honours reduced motion with no extra code), generating in CI (product screenshots change in a reviewable commit). Budget: `public/frames` + `public/og` <= 5 MB. | T41-lite, changelog 0014 | proposed |
| D-078 | 2026-09-19 | **The A/B bucket is `hash(sessionId() + slug) % live rows`**: deterministic per visitor, spread across visitors, no cookie, no server; `?variant=A|B` forces a side. Rejected: random per view (uncomparable events, the page changes under the visitor) and a stored flag (nothing to serve from yet). | changelog 0014, R-S04 | proposed |
| D-079 | 2026-09-19 | **`/p/:slug` renders the archetype the published row carries; the explicit paths (`/audit`, `/story`, `/letter`) force theirs.** Refines D-024's "the archetype in the URL wins over the snapshot": the bare path has no archetype in it, and without this a variant B published as an audit would render as a reveal and the test would be void. | changelog 0014 | proposed |
| D-080 | 2026-09-19 | **Static prerender of `/p/<slug>` at build time is the way to crawler-visible `og:` tags (a later task).** A HashRouter route cannot give a non-JS crawler per-page tags, so every prospect link unfurls with `index.html`'s today (`usePageMeta` fixes it once the SPA runs). Rejected: `BrowserRouter` (GitHub Pages has no rewrite rule, every deep link would 404). | changelog 0014 | proposed |
| D-081 | 2026-09-19 | **Domain terms stay as English loanwords across EN and ES** (Outreach, Intake, Demo, Kanban, Prompt(s), Assets, Roles, Slug, Hero, typography names, pricing tier names) rather than being force-translated, matching what earlier passes already did consistently. `scripts/i18n-check.mjs`'s exemption list encodes the mechanical part (proper nouns, codes, URLs, numbers, currency bands); the domain-loanword judgment call stays a human/agent review step against sibling keys, not a script rule, so a real future gap in one of those words is not silently exempted. | T30 | proposed |
| D-082 | 2026-09-19 | **`npm run qa:a11y` (axe-core WCAG 2.1 AA + a 40-step keyboard walk) is the accessibility QA script**, run the same way as `qa:responsive` (super admin, dev mode off, per built route x width) and reporting to `docs/qa/a11y-report.{md,json}`. Not a build gate yet. Rejected: a unit-test harness (duplicates the app-boot plumbing `qa-lib.mjs` already provides); hand-rolled contrast math (axe resolves stacked/transparent backgrounds correctly). | T32, P-01 | proposed |
| D-083 | 2026-09-19 | **The keyboard-walk's focus-ring check is DOM-only (`outline`/`box-shadow`) and known to false-positive on SVG components that signal focus via `stroke`/`fill`** (e.g. the plan dependency graph). Treat a `g`/`svg`-tagged focus-ring failure from this script as "needs a manual look," not as a confirmed gap, until the script grows an SVG-aware check. | T32 | proposed |
| D-084 | 2026-09-19 | **Version 0.3.0 = pass 2**: 51 routes (all built, L-06 hidden), 11 tables, 47 rules, 38 components, 136 unique action ids / WebMCP tools (210 declared rows), 18 engine checks, 35 plan tasks (23 done, 1 doing, 4 backlog, 6 awaiting Justin, 1 blocked). Phase 3 closed (T30..T33), phase 4 opened (T41-lite, T45, T47 doing, T49). | T21 (changelog 0016) | decided |
| D-085 | 2026-09-19 | **Every library component root carries `data-component="<Name>"`** (`componentAttr()` in `src/design/meta.ts`; `Icon` is the one exception, a glyph inside its host). Annotation selectors anchor at a component root only when it is unique on the page, otherwise they keep climbing with `:nth-of-type`, so a pin on the third Button of a row resolves to that Button. The class-hint table is gone. | T21, T49 (D-076) | decided |
| D-086 | 2026-09-19 | **Text and status tokens sit at >= 4.5:1 on their surfaces in both themes**: light muted `#5B6579` (6.0), label `#616B80` (5.35), faint `#66708A` (4.9, the floor); dark faint `#8C96AB`; status hues success `#0F6B3C`, warn `#9A5200`, danger `#B91C1C`, info `#1D4ED8` on their soft backgrounds, and the lifecycle hues follow. `.eyebrow`, `Badge`, `Stat`, `Field` labels inherit the fix. Rejected: per-page overrides (2013 axe instances came from six shared pairings). | T21, P-03 | decided |
| D-087 | 2026-09-19 | **A prospect's palette is theirs, so the text on it is computed, never assumed**: `prospectStyle()` sets `--lp-on-primary` / `--lp-on-accent` with `readableOn()` (white when it clears 4.5:1, else the prospect's text colour); `Avatar` initials do the same; `--lp-muted` is 72 % of their text and small primary-coloured copy uses `--lp-primary-text` (70 % primary + 30 % text). The seeded palettes stay as designed. | T21, R-D01 | decided |
| D-088 | 2026-09-19 | **44 px targets without visual growth**: `DataTable`'s row link is the first cell's 44 px anchor with an overlay that stops at cells holding their own control (`td:has(button, a, input, select)` is lifted above it); `Chip` remove, `Toggle` switch, `Toast` dismiss and badge / task links are 44 px boxes pulled into their rows with negative margins; interactive chips are at least 44 x 44; zoomable canvases (K-03 graph, D-07 canvas) never fit below the zoom at which a node or card control is 44 px tall, they scroll instead. Rejected: pseudo-element halos alone (a rect-based checker and a finger both measure the element). | T21, P-03 | decided |
| D-089 | 2026-09-19 | **The OS demo's phone chrome is two rows under 600 px**: wordmark + "Viewing as <role>" button (role list in a `Drawer` sheet) + compact `LangToggle`, then two 44 px CTAs; 109 px of chrome instead of 291. From 600 px up the one-row bar with the role `Select` stays, so `npm run frames` still reads the role slugs from it. | T21, C-01..C-07 | decided |
| D-090 | 2026-09-19 | **`IndustryKey` is `keyof typeof DEFS` in `src/engine/catalog/industries.ts`**; the key is injected from the property name, so adding an industry is one catalog property and no type edit. `src/engine/types.ts` re-exports the type. | T21, studio request | decided |
| D-091 | 2026-09-19 | **`npm run qa:a11y` judges a text-only link as inline text (WCAG 2.5.8 inline exception) even when a flex parent blockifies it, reads the focus ring inside a same-origin iframe (Chromium never matches `iframe:focus`), and reads an SVG `g` control's own `outline`.** Every rule is stated in the report header. The scan is not a build gate yet; the residue is named in `docs/qa/a11y-report.md` (0 at v0.3.0). | T21, T32 (D-082, D-083) | decided |
| D-092 | 2026-09-19 | **No strings outside module tables**: the TV hint is `hub.tv_hint`; `src/a11y` exports hooks only. `.modal-foot` wraps by default (each action takes the row under 480 px), so no module needs a footer override. | T21, P-13 | decided |
