import type { Rule } from './index';
/** R-D01..R-D03: the lead magnet itself. The demo is the product, so these three are never traded away. */
export const rules: Rule[] = [
  { id: 'R-D01', title: 'The demo is always themed from the prospect palette', description: 'The demo root carries prospectStyle() (--lp-*) and maps those onto the tokens library components read, so Card / Button / DataTable are theirs. Tokens are never forked per prospect and the demo never falls back to the Imagine palette.', category: 'demo', status: 'implemented', pages: ['C-01', 'C-02', 'C-03', 'C-04', 'C-05', 'C-06', 'C-07'], source: 'P-02, prompt 0001', implementedIn: 'src/modules/os-demo/demo.css + DemoShell' },
  { id: 'R-D02', title: 'Every role in business_roles + life_roles gets a view', description: 'deriveRoleViews(prospect) drives the role switcher, the departments board and the life page: no role is dropped, and the life roles (partner, kids, accountant, coach) are people with their own view, not settings.', category: 'demo', status: 'implemented', pages: ['C-01', 'C-02', 'C-03', 'C-06', 'C-07'], source: 'prompt 0001', implementedIn: 'src/engine/roles.ts + DemoShell role switcher' },
  { id: 'R-D03', title: 'The demo never asks for data before value', description: 'No form, no email wall, no sign-in before the workspace. "Save your workspace" is a persistent but unobtrusive pair with "Book a walkthrough" and only opens a name / email modal when the visitor asks for it; submitting writes a form_submit event.', category: 'demo', status: 'implemented', pages: ['C-01', 'C-02', 'C-03', 'C-04', 'C-05', 'C-06', 'C-07'], source: 'R-C01, conversion-playbook 1', implementedIn: 'DemoShell save modal' },
];
