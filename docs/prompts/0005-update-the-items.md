# 0005 - Update the items

date: 2026-09-20 21:07 UTC · from: Justin Massion via Slack #lead-magnets (thread of prompt 0001) · surface: engine / demo / landing / studio · follows: 0004

## Prompt (verbatim)

update the items, I have Hoy  in my Github, cal tenant law, and petrockhotel in there too... those all have pretty well built out operation systems

## Response

Reference items pass, shipped as **v0.5.0** (changelogs 0024..0026). Read as "the catalog now knows what a dog hotel, a tenant-law firm and a wellness club actually run - departments, KPIs, tools, a capacity gauge - because three real systems were read first, and the demo and the landing pages show that depth for three new prospects without changing a byte of the three existing ones".

**What shipped**

- **Reference study** (`docs/reference/reference-systems.md`, Fable 5.1): read-only shallow clones of `imagine-os/hoy` (v0.8.0, HOY Wellness Center - a wellness club, Spanish-first, 98 routes / 48 tables), `imagine-os/cal-tenant-law` (v0.1.2, California Tenant Law - a tenant-side firm run as a network of eight regional offices selling fixed-price services by eviction stage, 56 routes / 27 tables) and `imagine-os/petrock` (v0.2.0, Petrock Hotel & Spa - a two-location dog hotel with grooming, spa and daycare, 182 routes / 74 tables). For each: roles and permissions, departments, dashboards and KPIs, tables, the tools it replaces, its design system and docs conventions, then a mapping. **Mapping decision**: Petrock -> `pet_care` sub `dog_hotel_spa`, CTL -> `law_firm` sub `tenant_law`, Hoy -> `gym_wellness` sub `wellness_club`. No human `hospitality` industry key yet (D-129, proposed): a dog hotel is pet care with a hotel's operations (rooms, nights, occupancy, day buckets); a human hotel's stack is a PMS and a channel manager, a different industry, and adding it now would misfile Petrock next to it. Every business fact about a real company stays in its repo; every prospect below is fictional.
- **Catalog items** (Fable 5.1, changelog 0024): sub-industries 24 -> 27, distinct tools 114 -> 128, listings 352 -> 368, 23 industries unchanged. The depth (8 / 9 / 8 departments, 8 KPIs each, extra pains, motifs, roles, a `meter`) lives on the three new subs, not on the industry entries, so a generic daycare is still a daycare and Maya's, Daniel's and Priya's pages are byte-identical (snapshot-checked). The 14 tools the repos name (Squarespace, PetLinx, Time To Pet, Yelp Ads, WordPress hosting, Ecwid, Microsoft Teams, VoiceStamps, Jotform, WordPerfect, PayPal, WhatsApp Business (manual), Glofox, Momence, Gusto) enter dated `2026-09-20` with a `price_source_note` that starts "Estimate"; industry-wide prevalence stays under the rare gate wherever a seeded page would otherwise change. `docs/reference/catalog.md` regenerated with a depth block per sub.
- **Engine T55** (Fable 5.1, changelog 0024): `industryFor(p)` - the industry as the prospect experiences it (the sub's departments / KPIs / pains / motifs / roles / meter applied; the very same object when there is nothing to apply) - read by `deriveRoleViews()`, `composePage()`, `imagePrompts()` and the rule enricher; `candidateStack(p)` - the industry stack plus the sub's `extra_tools` at the sub's prevalence - is what `guessStack()` now draws from, so a dog hotel is guessed PetLinx and Squarespace where a nine-person daycare is asked; `WidgetKind` gains `meter` (`MeterSample { value, max, unit?, hint? }`), emitted for owner / partner / director / manager roles when the catalog declares a `MeterHint` (data, never a regex on KPI labels). Engine checks 21 -> 26.
- **Seeded prospects** (changelog 0024): `pro_camila` Camila Reyes, Fetch & Stay Dog Hotel, San Diego, `fetch-and-stay-san-diego`, reveal; `pro_alicia` Alicia Navarro, Renters' Shield Law, Fresno, `renters-shield-law-fresno`, letter, with a confirmed booking; `pro_valeria` Valeria Mendoza, Raíz Wellness Club, San Antonio, Spanish-first, `raiz-wellness-san-antonio`, reveal. Each with a live page, stack guesses, 11 asset prompts, a touch, events and the two intake turns that answered the sub-industry. `SEED_VERSION` 5 reseeds stale browsers and keeps rows a person created.
- **Demo depth T62** (Opus 5, changelog 0025): the `Meter` molecule (value, capacity, unit, percentage and a bar; `role="meter"` with `aria-valuetext` "50 / 58 rooms · 86%"; never colour alone; host-sized through `--meter-*` properties; the library's 43rd component), used by the C-02 widget card and by the landing device mockups; `DemoShell`, `HeroReveal` and `OgCard` read `industryFor(prospect)`; C-03 shows **one KPI per department** (eight columns for the hotel, nine for the firm, eight for the club, no repeats); a lead role's home leads with the gauge, the way Petrock's owner dashboard and Hoy's admin home open the day. **Measured** on nine routes at 390 / 1280 (overflow 0, no console errors) plus 360 / 768 / 1920 / 2560 / 3840 on C-01 / C-03, light and dark.
- **Integration** (Fable 5.1, changelog 0026): changelogs numbered, decisions D-129..D-141, frames regenerated for six prospects with a new `frames --stride=` flag so the public assets stay under 5 MB, OG cards for the three new slugs, the responsive and a11y matrices on the changed codes, screenshots, ops-manual chapter 02 in EN and ES, surfaces / README / CLAUDE.md / kanban / plan seed, v0.5.0.

**Gates at push**: `npm run build` green; 26 engine checks; 20 voice checks (144 phrases); `npm run i18n:check` 35 flags, the same documented loanwords / cognates / proper nouns as 0023 (0 new, 0 real gaps); `npm run qa:responsive` on 14 codes: 196 cells (14 routes x 7 widths x light / dark) / 0 failing; `npm run qa:a11y` on 6 codes: 0 axe violations, 0 focus failures, 0 hit-target failures (6 routes x 390 / 1280, preview, settled); frames + og 4,742,887 bytes (six prospects, 3.97 MB + 0.55 MB; was 4.79 MB for three); 20 files for C-01, C-02, C-03, L-01, L-04, S-02, D-02 at 390 / 1280, dark for C-01, L-01 and D-02 (the script's key pages), no console errors, `docs/screenshots/routes.json` regenerated. Live: https://imagine-os.github.io/lead-magnet/ · code commit 0c7397d (the docs commit is the one after it).

**Needs Justin**

- New `proposed` decisions from this pass: **D-129..D-139** - dog hotels under `pet_care/dog_hotel_spa` and no `hospitality` key yet; the 14 estimated tool prices; three more seeded prospects; depth on the sub-industry via `industryFor`; the `meter` widget; `guessStack()` over `candidateStack`; the replacement-map seam deferred to T63; `Meter` never `ProgressBar`; lead roles lead with the gauge; components sized by their host through named properties; one KPI per department.
- **T64 - the 14 estimated tool prices to confirm** (every `price_source_note` starting "Estimate"): Squarespace $23, PetLinx $69 / location, Time To Pet $45, Yelp Ads $150 (ad budget), WordPress hosting $30, Ecwid $35, Microsoft Teams $4 / seat, VoiceStamps $30, Jotform $39, WordPerfect $15 (licence stand-in), PayPal $25 (fees stand-in), WhatsApp Business (manual) $5 (the phone it lives on), Glofox $110, Momence $99, Gusto $80. Confirm, correct, or tell us which ones you actually pay and we lock them as confirmed rows.
- Should a **human-hospitality industry key** exist now for a real hotel prospect, or only when one appears? Today a hotel would have to be filed under `pet_care`, which is wrong; nothing else is affected until then.
- Still open: D-106 (sample testimonial), D-108 (arrow keys on public pages at every width), D-110 (A/B gate), D-011 / D-013 / D-014 / D-015 and the D-056..D-083 and D-093..D-120 ranges; providers not wired, each a `Placeholder` that names it: T40 image generation, T42 booking, T43 LLM enrichment, T44 Supabase + auth, T51 Stripe, T54 static prerender (D-080), T56 comms.

**Model**: Fable 5.1 did the study, the engine / catalog / seed pass and the integration (docs, QA, frames, push); Opus 5 built the demo depth (`Meter`, `industryFor` in the renderers, one KPI per department). Every commit names its model.
