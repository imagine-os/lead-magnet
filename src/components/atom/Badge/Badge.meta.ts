import { createElement as h } from 'react';
import { defineMeta } from '../../../design/meta';
import { Badge } from './Badge';
export default defineMeta({ tier: 'atom', name: 'Badge', description: 'Pill for statuses, counts and page codes. `status` keys map to lifecycle hue tokens.', props: [{ name: 'tone', type: 'BadgeTone', default: 'neutral', description: '' }, { name: 'status', type: 'string', description: 'built | stub | live | draft | expired | backlog | doing | done | blocked | awaiting_justin' }], states: ['default'],
  usages: [{ title: 'Tones', render: () => h('div', { className: 'row wrap' }, h(Badge, null, 'Neutral'), h(Badge, { tone: 'primary' }, 'HUB-01'), h(Badge, { tone: 'success' }, 'Live'), h(Badge, { tone: 'warn' }, 'Stub'), h(Badge, { tone: 'danger' }, 'Blocked'), h(Badge, { tone: 'accent' }, 'New'), h(Badge, { tone: 'ink' }, 'Fable 5.1')) }, { title: 'Lifecycle statuses', render: () => h('div', { className: 'row wrap' }, ...['built', 'stub', 'live', 'draft', 'expired', 'backlog', 'doing', 'done', 'blocked', 'awaiting_justin'].map((s) => h(Badge, { key: s, status: s }, s))) }], a11y: ['Text carries the meaning; colour is never the only signal.'], usedBy: ['HUB-01', 'D-01', 'D-07'] });
