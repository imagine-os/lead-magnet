import { createElement as h } from 'react';
import { defineMeta } from '../../../design/meta';
import { Card } from './Card';
export default defineMeta({ tier: 'molecule', name: 'Card', description: 'Surface container with padding and tone variants; tone="prospect" paints it from the page\'s --lp-* properties.', props: [{ name: 'padding', type: "'none'|'sm'|'md'|'lg'", default: 'md', description: '' }, { name: 'tone', type: "'surface'|'tint'|'ink'|'prospect'", default: 'surface', description: 'prospect: --lp-surface / --lp-text / line from --lp-text 14 % (fallback --color-*)' }, { name: 'interactive', type: 'boolean', description: 'Hover / focus-within lift' }], states: ['default', 'interactive', 'prospect'],
  usages: [{ title: 'Tones', render: () => h('div', { className: 'grid grid-4' }, h(Card, null, 'Surface'), h(Card, { tone: 'tint' }, 'Tint'), h(Card, { tone: 'ink' }, 'Ink'), h(Card, { tone: 'prospect', style: { '--lp-surface': '#FFF8F2', '--lp-text': '#2B1C14', '--lp-bg': '#F4E9DF' } as Record<string, string> }, 'Prospect')) }], a11y: ['Decorative container; interactive cards contain a real link or button.'], usedBy: ['HUB-01', 'D-02', 'D-07', 'L-01'] });
