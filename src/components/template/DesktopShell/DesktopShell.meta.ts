import { createElement as h } from 'react';
import { defineMeta } from '../../../design/meta';
export default defineMeta({ tier: 'template', name: 'DesktopShell', description: 'Staff shell for studio / admin / plan / dev / docs / manual: sidebar from route nav defs (drawer under 900 px), top bar, content, FeedbackButton.', props: [{ name: 'surfaces', type: 'Surface[]', required: true, description: 'Which surfaces feed the sidebar' }, { name: 'title', type: 'string', required: true, description: '' }, { name: 'feedback', type: 'boolean', default: 'true', description: 'Mount the FeedbackButton' }], states: ['wide', 'narrow drawer'], usages: [{ title: 'You are looking at it', render: () => h('p', { className: 'muted small' }, 'This page renders inside DesktopShell. Resize under 900 px for the drawer.') }], a11y: ['<main id=main>; nav labelled; drawer closes on Esc-equivalent close button.', 'useSpatialNav + useGamepadNav on the shell root: arrows / d-pad move focus across sidebar, top bar and content; Escape or Backspace close the drawer, else go one hash level up; regions that own their arrows carry data-spatial="skip".'], usedBy: ['D-01'] });
