import { createElement as h } from 'react';
import { defineMeta } from '../../../design/meta';
import { LangToggle } from './LangToggle';
export default defineMeta({ tier: 'molecule', name: 'LangToggle', description: 'EN / ES switch bound to I18nProvider.', props: [{ name: 'size', type: "'sm'|'md'", default: 'md', description: 'Both sizes keep the 44 px touch floor; sm tightens the padding.' }, { name: 'compact', type: 'boolean', default: 'false', description: 'One 44 px button that names the language it switches to; for phone chrome where the EN | ES group does not fit.' }], states: ['en', 'es', 'compact'], usages: [{ title: 'Default', render: () => h(LangToggle) }, { title: 'Compact (phone chrome)', render: () => h(LangToggle, { compact: true }) }], a11y: ['role=group; aria-pressed on each option; lang attribute on labels.', 'compact: a single button whose aria-label is written in the target language ("Cambiar a español" / "Switch to English").'], usedBy: ['HUB-01', 'C-01'] });
