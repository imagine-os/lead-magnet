import type { Rule } from './index';
/** Plan rules (module `plan`, task T10). The plan is a dependency graph, not a calendar. */
export const rules: Rule[] = [
  { id: 'R-K01', title: 'Nothing is done while a dependency is not done', description: 'A task can only move to done when every task in its depends_on is done. blocked and awaiting_justin are always allowed because they explain themselves; the Kanban and the task detail refuse the move and name the missing dependencies.', category: 'plan', status: 'implemented', pages: ['K-01', 'K-02', 'K-03', 'K-04'], source: 'build-plan: tasks are bound by dependencies, not calendar days', implementedIn: 'src/modules/plan/taskModel.ts statusChange()' },
  { id: 'R-K02', title: 'Every task names the model that does it', description: 'tasks.model is one of Fable 5.1 (judgment, architecture, shared code), Opus 5 (modules and pages), Sonnet 5 (mechanical passes) or Justin (his call). Every card, row, node and detail header shows it as a distinctly toned badge, and every reply names the model that did the work.', category: 'plan', status: 'implemented', pages: ['K-01', 'K-02', 'K-03', 'K-04'], source: 'org policy: model routing', implementedIn: 'src/modules/plan/taskModel.ts MODEL_TONE' },
  { id: 'R-K03', title: 'Status vocabulary is exactly five lanes', description: 'backlog, doing, done, blocked, awaiting_justin - no others. awaiting_justin means a human decision is the blocker (provider, key, budget); blocked means another task or an external fact is. Lanes, filters and the status Select all read TASK_STATUS, so the vocabulary cannot drift.', category: 'plan', status: 'implemented', pages: ['K-01', 'K-02', 'K-04'], source: 'src/data/schema/core.ts TASK_STATUS', implementedIn: 'src/modules/plan/taskModel.ts LANES' },
  { id: 'R-K04', title: 'Ready = all dependencies done', description: 'A task is ready to start when every dependency is done (none = ready). The Kanban marks ready cards, the list sorts on it and the timeline dims what is still waiting, so the next thing to pick up is never a guess. done_at is stamped on the move to done and cleared on any move out of it.', category: 'plan', status: 'implemented', pages: ['K-01', 'K-02', 'K-03'], source: 'build-plan order of operations', implementedIn: 'src/modules/plan/taskModel.ts readiness() / statusPatch()' },
];
