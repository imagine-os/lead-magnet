import type { Rule } from './index';
export const rules: Rule[] = [
  { id: 'R-A01', title: 'One funnel vocabulary, in order', description: 'The funnel is always outreach_open -> view -> demo_open -> booking_started -> booking_confirmed, counted as distinct sessions per stage so a chatty page cannot inflate a step. Every breakdown (archetype, warmth, variant) uses the same five stages in the same order; no page invents a stage.', category: 'analytics', status: 'implemented', pages: ['A-01', 'A-02'], source: 'conversion-playbook 9, prompt 0001 T15', implementedIn: 'src/modules/admin/funnel.ts FUNNEL_STAGES' },
  { id: 'R-A02', title: 'A/B needs both variants live', description: 'An A/B readout exists only for a slug with two live pages rows (variant A and B, D-061); with no such slug the card says so instead of implying a winner. Both sides are counted with the shared funnel code on one shared scale, variants are never pooled across slugs, and every rate is printed with the two session counts behind it.', category: 'analytics', status: 'implemented', pages: ['A-01', 'A-03'], source: 'P-15 (A/B on cold traffic), D-061', implementedIn: 'src/modules/admin/ab.ts + AbReadout.tsx' },
  { id: 'R-A03', title: 'Recommendations are recorded before they are applied', description: 'adaptFromEvents() output is written to `recommendations` (status proposed, with reason, score and who decided) before any page row changes; applying flips that row to applied with decided_at, and dismissing flips it to dismissed. Nothing is changed on the strength of an on-screen suggestion alone (P-08).', category: 'analytics', status: 'implemented', pages: ['A-02'], source: 'P-08, P-11', implementedIn: 'src/modules/admin/ProspectTimelinePage.tsx record() and AbReadout.tsx recordWinner()' },
  { id: 'R-A04', title: 'No winner below the minimum sample', description: 'An A/B side is only called ahead when BOTH variants have at least AB_MIN_SESSIONS (30) distinct view sessions AND the leader is at least AB_MIN_LIFT (10 %) better in relative terms on view -> demo_open, the page\'s own job. Below the sample the readout says how far off it is and shows every rate with its counts; between the sample and the lift it says "too close to call". Bookings are shown but never decide - they are far too sparse per page.', category: 'analytics', status: 'implemented', pages: ['A-01'], source: 'P-15, prompt 0004 T53', implementedIn: 'src/modules/admin/ab.ts abReadout()' },
];
