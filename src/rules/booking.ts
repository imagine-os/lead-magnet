import type { Rule } from './index';
export const rules: Rule[] = [
  { id: 'R-B01', title: 'Slot before details', description: 'B-01 asks for a time first; the name / email form is inert (and says so) until a slot is selected, so we never gate the calendar behind a form. A ?slot= in the link counts as the selection.', category: 'booking', status: 'implemented', pages: ['B-01'], source: 'conversion-playbook 1 (R-C01), prompt 0001 T14', implementedIn: 'src/modules/booking/BookPage.tsx' },
  { id: 'R-B02', title: 'Fifteen minutes is the default', description: 'Every slot is 15 minutes and every bookings row is written with duration_min = 15; the grid is built in 15-minute steps inside working hours (09:00-17:00, lunch blocked, weekends closed) in the prospect\'s likely timezone from their city.', category: 'booking', status: 'implemented', pages: ['B-01', 'B-02'], source: 'PageModel.cta secondary "Book a 15-min walkthrough"', implementedIn: 'src/modules/booking/slots.ts DURATION_MIN' },
  { id: 'R-B03', title: 'Bookings are written by id through the provider', description: 'Confirm inserts one bookings row (status requested) through DataProvider.insert and tracks booking_confirmed with that id; B-02 reads the row back by id. No booking state lives only in component memory (P-14).', category: 'booking', status: 'implemented', pages: ['B-01', 'B-02'], source: 'P-14', implementedIn: 'src/modules/booking/BookPage.tsx confirm()' },
];
