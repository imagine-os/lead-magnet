# 0026 - Integration, reference items: v0.5.0

version: 0.5.0
date: 2026-09-20
prompt: 0005
intent: Take the two reference-items change sets (0024 engine / catalog / seeds by Fable 5.1, 0025 demo depth by Opus 5) and the study doc (`docs/reference/reference-systems.md`), gate them (typecheck, engine, voice, i18n, build), regenerate the public assets for six prospects within the 5 MB budget, re-run the responsive and a11y matrices on the changed codes, take screenshots, land every doc in the same turn (prompt 0005 with its response, changelogs numbered, decisions, kanban, build plan + plan seed, surfaces, README maps, CLAUDE.md, ops manual, page docs), bump to v0.5.0 and push.
decision: (1) the workers' guessed decision numbers hold - D-128 was the tail, so 0024's rows are D-129..D-135 and 0025's D-136..D-139 (`proposed`); the integration adds D-140 (the version row) and D-141 (the frame budget holds by count: `frames --stride=`); (2) the frame budget is met by halving the tour with a new `--stride=2` flag rather than by JPEG quality alone - six prospects at 0023's q70/47 would have been ~9 MB, at q52/36 still 7.6 MB, and text in the phone chrome smears under q50 (D-141); (3) ops-manual chapter 02 is the one chapter that changed meaning (the industry no longer alone seeds roles / KPIs / motifs when a sub-industry with depth exists), so it is fixed in EN and ES and bumped to 0.5.0; chapter 03 stays true and untouched; (4) no route, table, rule, action, DataProvider method or npm script was added, so §1.4 / §1.6 of `surfaces.md` are unchanged (144 tools / 230 declared rows); the `frames` script gained a flag, recorded in §1.3.
rejected: raising the 5 MB budget for six prospects (the public pages' first load pays it, D-077); frames for only the three new prospects at full quality (the three original landing pages would fall back to the live composition and stop showing the recorded demo); renumbering the workers' D- guesses to leave a gap for the integration rows (append-only means the next free number, whoever wrote the row); editing ops-manual chapter 03 for "three seeded prospects" (it never said so - its `{{archetypes}}` directive reads the live table).
files: scripts/frames.mjs (`--stride=`), public/frames/** (six prospects, 132 files), public/og/*.jpg (six slugs), docs/prompts/0005-update-the-items.md (new), docs/changelog/{0024-reference-items.md, 0025-os-demo-depth.md} (numbered from `_pending`, integration notes), docs/changelog/0026-integration-reference-items.md (new), docs/decisions.md (D-129..D-141), docs/kanban.md, docs/build-plan.md + src/data/seed/plan.ts (T55 / T62 done, T61 / T63 backlog, T64 awaiting Justin, "Reference items outcome"), docs/reference/surfaces.md (§1.1, §1.2, §1.3, §3), docs/README.md (`reference/reference-systems.md` row), README.md (counts, 0.5.0), CLAUDE.md (six seed ids, `industryFor` / `candidateStack` in the engine contract), docs/ops-manual/{en,es}/02-intake-and-research.md (0.5.0), docs/pages/{C-01, C-02, C-03, L-01, S-02, D-02}.md, docs/qa/README.md, docs/qa/{responsive-report, a11y-report}.{md,json} (regenerated for the changed codes), docs/screenshots/{C-01, C-02, C-03, L-01, L-04, S-02, D-02}/* + routes.json, package.json (0.5.0)
codes: C-01..C-07 L-01..L-04 S-01 S-02 D-02 (QA) · C-01 C-02 C-03 L-01 L-04 S-02 D-02 (screenshots) · L-01 L-03 L-06 (frames / og for six prospects) · M-02 (manual chapter 02)

## What changed at the root

- **Frames for six prospects within budget.** `scripts/frames.mjs` gains `--stride=N`: every Nth scroll step of each tour stop is kept (always the first), so the phone tour is 14 frames and the laptop tour 6 at `--stride=2` (24 / 12 at stride 1). The landing already reads `count` / `desk_count` from `index.json`, so nothing else changed. Run: `npm run frames -- --stride=2 --quality=55 --desk-quality=38` -> 132 files, **4,170,640 bytes** (pro_maya 667 KB, pro_daniel 687 KB, pro_priya 686 KB, pro_camila 685 KB, pro_alicia 671 KB, pro_valeria 673 KB; roles read from the demo itself, e.g. owner / manager / front-desk, owner-attorney / attorney / paralegal, owner / coordinator / front-desk); `npm run og` -> six cards at q82, **572,247 bytes**, no console errors. Together **4,742,887 bytes** (4.52 MB; 4,790,816 for three prospects in 0023). Calibration: one prospect at stride 1 q52/36 was 1,266,014 bytes, i.e. 7.6 MB for six.
- **No code change was needed in the two change sets.** Typecheck was clean at the start; `test:engine` 26 / 26 and `test:voice` 20 / 20 on the merged tree; `i18n:check` 35 flags, the identical D-081 set as 0023 (the new module string `demo.near_capacity` "near capacity" / "casi al tope" and `demo.kind_meter` "Meter" / "Medidor" are translated). The interim `widgets.tsx` / `os-demo/index.ts` / `MiniOs.tsx` edits from 0024 were superseded by 0025 in the same tree and are committed with it.
- **Ops manual chapter 02** (EN + ES, version 0.5.0): "Pick the industry carefully ... it seeds the roles, the pains, the KPIs and the imagery motifs" now continues with what happens when the catalog gives the industry a sub-industry with depth (the sub takes over that seeding; its tools are guessed first; the three seeded examples). Chapter 03 read in full: nothing false (its archetype table is a live directive).
- **Docs**: `CLAUDE.md` seed ids (six) and engine contract (`industryFor`, `candidateStack`); `docs/README.md` row for `reference-systems.md`; root `README.md` counts; page docs C-01..C-03 (heading repointed at 0025, changelog bullet), L-01, S-02, D-02 (reference items bullets); `docs/qa/README.md` (the six-prospect frames setting).

## Gates and evidence
- `npm run typecheck` clean; `npm run test:engine` **26 / 26**; `npm run test:voice` **20 / 20** (144 phrases); `npm run i18n:check` **35 flags**, 0 new, 0 real gaps; `npm run build` green (tokens -> tsc -> vite) before the frames run and again before the push.
- `npm run qa:responsive -- --codes=C-01..C-07,L-01..L-04,S-01,S-02,D-02`: 196 cells (14 routes x 7 widths x light / dark) / 0 failing.
- `npm run qa:a11y -- --codes=C-01,C-02,C-03,L-01,L-04,D-02` (alone, after the responsive run): 0 axe violations, 0 focus failures, 0 hit-target failures (6 routes x 390 / 1280, preview, settled).
- Screenshots: 20 files for C-01, C-02, C-03, L-01, L-04, S-02, D-02 at 390 / 1280, dark for C-01, L-01 and D-02 (the script's key pages), no console errors, `docs/screenshots/routes.json` regenerated. The script has no prospect / slug flag (`--codes`, `--only`, `--widths`, `--dark`, `--dev`, `--label`, `--smoke`, `--port`), so C-01 / C-03 for `pro_camila` and the three new landing slugs were not captured by it; the frames run (`public/frames/pro_camila/`, `pro_alicia`, `pro_valeria`) and the OG cards are the visual evidence for the new prospects, and a per-code param override is a kanban card.
- Counts at v0.5.0 (D-140): 51 routes (all built), 11 tables, 51 rules, 43 components (+ `Meter`), 144 unique action ids / WebMCP tools (230 declared rows, unchanged), 26 engine checks, 20 voice checks, 46 plan tasks (30 done, 0 doing, 7 backlog, 8 awaiting Justin, 1 blocked), six seeded prospects, eight pages, 27 sub-industries, 128 distinct tools, `SEED_VERSION` 5.

## Docs same turn
Prompt 0005 (verbatim, with the response); changelogs 0024 / 0025 numbered from `_pending` with an integration note each (`_pending/` holds only its README); `decisions.md` D-129..D-139 (module rows, `proposed`) and D-140..D-141 (integration, `decided`); `kanban.md` (T55, T62 done; T61, T63 backlog; T64 and the hospitality-key question awaiting Justin; the frames QA card carries the six-prospect setting); `build-plan.md` phase-4 line, tasks table, "Reference items outcome" mirrored in `src/data/seed/plan.ts` (T62 / T61 / T63 / T64 depend on T55, which is done - the dependency rule holds); `surfaces.md` §1.1 / §1.2 / §1.3 / §3; `docs/README.md`, root `README.md`, `CLAUDE.md`, `docs/qa/README.md`; page docs C-01..C-03, L-01, S-02, D-02; ops manual 02 EN / ES; `package.json` 0.5.0.

## Requests carried forward (kanban)
- T61 studio sub-industry surfaces (S-01 filter, S-02 read-only depth chips, the `Meter` for the capacity hint); T63 `StackGuess.kind` + `seam` from CTL's replacement map (D-135); T64 price review of the 14 estimates (awaiting Justin, D-130); a human-hospitality industry key only when Justin names a real hotel prospect (D-129).
- QA: a per-code param override in `screenshots.mjs` / `qa-dpad.mjs` (the B-02 fixture card already asks for it) so `pro_camila` C-02 (meter) and `pro_alicia` C-03 (nine columns) can be captured; `qa:dpad` re-run on the full tree next sweep (not part of this pass's matrix).
- From 0024 / 0025, unchanged: `MeterSample.unit` is optional and rendered when present - nobody should "fix" it away; `departments[].kpi?` only if a sub ever has fewer KPIs than departments.

## Kanban moves
T55 -> done (Fable 5.1, changelog 0024); T62 added and done (Opus 5, changelog 0025); T61, T63 -> backlog; T64 -> awaiting Justin; the reference-systems study and this integration -> done; "Awaiting Justin" gains D-129..D-139, T64 and the hospitality-key question.
